# 2026-09-11 11:56 CST — 董秘问答与指数/基金波次固定发布

- 云端正常 `publish_only` 在 11:37:45 CST 原子发布 `data-82ece8a2297797f1f2508f4a782d897164d910a650b45c06df87ae8c24bb1838`；manifest 301007830 字节，实算 SHA 与 release ID 一致。
- 固定版闭包把四份冻结清单的1440个任务连接到权威终态结果与内容寻址文件，再以 `jq --stream` 单遍核对release文件映射。1440 object、1440 observation、1169 Parquet共4049个唯一引用、743244226字节全部进入发行版，元数据、大小和物理SHA错误均为0。authority闭包 `validation/release-82ece8a-exact-wave-closure-20260911.json` SHA256为`d0ba398fb7bdfe8dda0c7e1b57d4076b62e3157d75d77edeff76b073a91f8346`。
- Mac LaunchAgent第168轮exit0、stderr为空，CURRENT追平同一release；客户端报告778375个发行引用。独立重算本轮4049个文件全部通过，镜像约93GiB。
- Mac生产镜像容器在`--network none`、socket/DNS阻断、两个Token变量为空和镜像只读条件下，`index_daily`、`fund_nav`、`fund_share`、`irm_qa_sh`、`irm_qa_sz`各读3行，上游调用0。
- 手动使用`launchctl kickstart -k`可能中断了一个已在运行、已预填内容寻址文件但尚未记日志的自动客户端，因此第168轮`downloaded_files=0`不能解释为没有传输。最终CURRENT、客户端全量校验和独立4049文件SHA均通过；以后手动触发前先看state，运行中不使用`-k`。
- `bdd0fd98`补齐共享基金exact runner的`would_write=false`，Mac和云端各10项测试通过，无需重启。最初Python正则固定版扫描因CPU回溯只读终止，换成`jq --stream`后14.5秒完成；未留下部分闭包。
- API、Tushare worker和Beat均healthy、restart0、OOM false，云盘可用228029927424字节。完整历史、修订、known_at、PIT与RRG准入缺口继续开放；整机快照timer和原始文档worker继续等扩盘/headroom复核。
- 机器证据：`docs/tushare-fixed-release-82ece8a-wave.evidence.json`。
