# Mac Tushare 附件登记 50 档灰度开始

- 时间、节点、任务：2026-09-18T13:40:01Z，Mac，registration50
- 状态：只准备更改私有生产配置，尚未替换生产值。
- 范围：将 `document_registration_max_observations` 从 100 灰度降到 50；不改登记持久游标、`partial_attempt`、`record_offset`、20 万条记录上限、90 秒配置截止、文档下载/解析、Tushare 限频或数据存储角色。

当前登记游标为 326418，attempt 最大 rowid 为 714707，约滞后 38.8 万条。最近八轮每轮登记 100 个附件 observation，扫描 5172–7440 条 attempt，耗时 8.877–17.468 秒；同期每轮仅新增约 400–540 个 attempt，因此降到 50 后预期仍可保持游标净追赶。文档待下载已超过 440 万，短期限制是下载/解析与结构化采集，不是 URL 登记数量。

生产通过原子移出 `ENABLED` 在自然周期边界替换私有配置，不重启 LaunchAgent。配置指纹变化会产生一轮预期的 `planning_only`，验收与真实采集轮分开。至少观察三个真实周期的登记扫描量、游标增量、周期总耗时、Tushare 请求和失败阶段；如果游标不再净追赶或总吞吐无收益，则恢复 100。
