# Tushare 发布与 identifiers 尾延迟审计就绪

- 分支 `codex/tushare-publish-identifiers-tail-20260910`，审计基线 `957d060b9e19a14125854239a7dc5a90c04980a3`，提交前已快进到最新 `origin/master` `e48656df`，其间无相关文件变化。本轮没有保留运行代码改动；只提交开始/就绪记录和 `docs/tushare-publish-identifiers-tail-audit.evidence.json`。未访问生产、Token、上游，未改 Celery 超时、限速、批量、配置、数据库或共享主工作树。
- 发布当前已使用 CPython C one-shot JSON chunks，逐块 UTF-8 hash/write/fsync 到私有临时文件，校验不可变目标并 fsync release 目录，再持久 publication intent、原子切 CURRENT；旧 CURRENT 和中断清理语义完整。500000 files + 1500000 closure children + 125000 gaps 的 BMP fixture 输出 `204389128` 字节，与生产暴露的 `206530431` 字节同量级。当前三次 0.740458/0.756506/0.773414 秒，中位 0.756506 秒、最大峰值 RSS 845512704 字节。
- 一个按顶层字段分段调用同一 C encoder 的原型保持全部字节和 SHA 完全相同，三次 0.745213/0.750919/0.773559 秒，中位仅下降 0.739%，最大峰值 RSS 796393472 字节、下降 5.810%。时间变化小于 1% 且落在重复波动内，不能解释 160 秒尾部；原型已撤销，避免增加第二条序列化路径和改变既有 encoder 诊断。当前发布代码不变。
- `identifiers()` 当前仍完整扫描 jobs/attempts，保留请求敏感结果、不可变对象 stat、同次 body 去重、发现字段投影和最终排序。新 68,543,176 字节/4×6000×342 宽体 fixture 中，已部署投影与全字段基线 identifier SHA 完全一致，0.338852 对 0.502390 秒，tracemalloc 峰值 101952090 对 178922457 字节，说明已部署优化有效但仍须完整解码源 JSON。
- 将当前 SQLite `UNION` 改成两段查询并由 Python 精确去重的原型在 200000 条元数据/66667 条选中结果上，中位 0.225206→0.236932 秒（慢 5.207%），tracemalloc 峰值 37060864→51307254 字节（高 38.442%），已撤销。之前完整固定版 30909 个原始对象/1,151,118,880 字节复放也已证明持久投影缓存默认 cold 34.398→35.798 秒，只有先花 70.911 秒完整 priming 后才到 23.065 秒，且没有生产内存余量证明；本轮不复活该候选。
- 证据 SHA256 `adce7a10a1060a638b7c994d81d404705c1e09a85cd9a1afd76c779063f00c4d`。49 项 Python3.10 发布/恢复/CURRENT/发现/规划专项回归通过（1.156 秒），清单字节等价、序列化/fsync/rename/pointer 失败、三代 archive、发现投影和计时、规划间隔均覆盖；Ruff 与 `git diff --check` 通过。
- 结论：当前证据不支持新的安全默认代码。下一次自然到期发布应直接保存已有 `publish_timing.serialization` 的 C chunk generation、总写盘/fsync及 retain/closure 各阶段；不能用最后 Python frame 把全部 160 秒归因于序列化。下一次 planning-only 应保存已有 discovery results/body_reads/duplicates，并只增加进程/cgroup峰值观测；若 identifiers 再接近上限，先隔离 SQL、stat、raw decode、merge 时间。跨任务可恢复扫描只有在定义能检测 job 更新/删除/恢复和对象替换的精确 source generation/SQLite snapshot 合同后才安全，部分聚合不得进入 planning。
