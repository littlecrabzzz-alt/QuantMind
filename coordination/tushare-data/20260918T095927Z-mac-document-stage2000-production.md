# Tushare：文档阶段 2000 生产验收

- 时间、节点、任务：2026-09-18T09:59:27Z，Mac，document-stage2000
- 状态：已验收并保留 `document_worker_max_documents=2000`；全量历史同步仍在进行
- 代码：`master` / `ccf6b44def55e6b26794f2100deb4beb5563b49e`

## 配置与部署

四路解析上线后连续五个真实生产轮次均达到原 1800 阶段上限且无失败，因此只将私有阶段额度提高到代码已有的 2000 硬上限。部署前原子移出 `ENABLED`，等待在途周期完成且 worker 明确写出 `disabled`；随后原子更新配置并原样恢复 marker，没有重启 LaunchAgent、移动数据库或修改 Tushare 限速。

- 配置 SHA-256：`bae4b800925a5812702fd479bb1308ce32046a7c77693de1ea623ca1bae39aa2` → `38e5f67f7f75b57d4f0cd43977309f46a0072cdbc2f300a023cb96fd60ad72b8`，权限保持 `0600`。
- `ENABLED` SHA-256 保持 `6b45163b577df25f4e6842501fc95128649c5b129daddf3958224864008eda40`。
- 100 秒工作截止、6 路下载、4 路解析、独立文档进程、500 次/分钟账户上限、接口分层限速和日配额均未改变。

## 真实生产吞吐

| 周期开始 | 文档阶段 | 下载 / 解析 | Tushare 请求 | 解析任务时间 / 槽容量 | 峰值进程树 RSS | 失败 |
| --- | ---: | ---: | ---: | ---: | ---: | --- |
| 09:53:29Z | 1,853 | 915 / 938 | 548 | 374.649 / 376.947s | 1,153.7 MiB | 无 |
| 09:55:24Z | 2,000 | 952 / 1,048 | 543 | 317.963 / 354.911s | 1,142.5 MiB | 无 |
| 09:57:15Z | 2,000 | 997 / 1,003 | 446 | 366.065 / 369.003s | 1,088.2 MiB | 无 |

三轮平均 1951 阶段，比原上限下已验收的 1800 提高约 8.4%。两轮完整利用新增额度；第一轮因文档较慢而由 100 秒截止收口，证明阶段上限没有放松时间边界。每轮 `processed` 都等于 `finish_db_calls`，没有遗留 claim 或失败阶段。

验收后 `Pages throttled=0`，数据卷约 2.2 TiB 可用；LaunchAgent 保持同一 PID、`runs=1`、从未退出，错误日志没有新增内容。

## 当前进度与下一瓶颈

最新原子状态为结构化 `done=351119`、`pending=2872637`、`empty=301484`、`blocked=871`、`permission_blocked=4784`、`split_pending=9847`；文档为 `pending=2125555`、`parse_pending=103644`、`parsed=87755`。文档登记仍会从旧 attempt 显式化新 URL，因此待办总数可暂时增长。

结构化采集继续使用单 HTTP worker、深度 2、独立进程和严格 500 次/分钟账户门；本次三轮实际请求为 446–548，采集阶段约 101 秒。后续应从账户门请求/睡眠计时和接口级频控中定位低于理论档位的原因，不直接提高官方上限。

结论：保留 2000 阶段额度。Mac 继续作为唯一全量归档写入者，云端仍只保留研究子集/缓存；队列仍为百万级，本次验收不代表全量完成。
