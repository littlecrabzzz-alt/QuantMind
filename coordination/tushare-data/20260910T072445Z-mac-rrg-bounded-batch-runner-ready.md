# RRG exact bounded batch runner: ready

- Candidate: `37ac51e0b6d40f6387511fcbe447ea426ee05145` on `codex/rrg-bounded-batch-runner`, rebased onto `origin/master` `ff21a3172113e9fdc42d35970f9c5f198028883c`.
- Audit: the pinned 1,767-job snapshot is `fund_div` 4 empty/1,714 pending and `etf_limit` 6 split_pending/43 pending; 1,765 jobs are priority 24. Normal family/API rotation still had not selected them, and ordinary `Pipeline.run()` cannot restrict its global expansion, split resume, reconciliation and selection to a manifest.
- Change: optional `Pipeline.run(..., task_ids=...)` uses a connection-local temporary set, selects only pending members, and round-robins APIs across their original `market` and `cross_asset_extra` groups. Default calls keep the prior response and scheduling path. Exact runs skip only global pre-work/expansion; selected responses retain current account/API reservations, observed cooldowns, daily quota, raw capture, normalization, attempts/retries, pagination, split and child reconciliation semantics.
- Helper: `scripts/run_tushare_rrg_acquisition_batch.py`, SHA-256 `cf5bffec76246cb414a4df18a80d8353af061613da0584af4d616bae05f98a4d`, defaults to plan-only. Execute requires verified authority, regular ENABLED/config/database/current files, nonblocking exclusive `pipeline.lock`, schema 6, full source/batch hash chain, all-task-ID hash, config hash, helper hash and 100 GiB free reserve before reading the Token.
- Bounds: rejects more than 360 upstream calls or 90 seconds, has an independent wall-clock alarm, remains SIGINT/SIGTERM interruptible, checkpoints each response through the existing Pipeline, never selects newly derived child IDs in the same exact run, and does not publish or switch `CURRENT`.
- Validation after rebase: 50 selected tests passed under temporary uv CPython 3.12.12 with DuckDB/PyArrow/httpx; Ruff check passed; Ruff format check passed for both new Python files; JSON, py_compile and diff checks passed. Local Python 3.10 was unavailable. No production, credential or upstream access occurred.
- Exact remaining gates: merge and deploy; mount the pinned batch/audit read-only; verify plan-only output; pin deployed helper SHA plus live config SHA; run one explicit authority batch capped at 360/90; retain and review the redacted receipt and API/state deltas; publish later only through the ordinary reviewed fixed-release path. RRG remains `blocked_data` for PIT membership/mapping and price/tradability/PCF gaps.
