# 消费公平调度发布窗口

root接管main Git，本轮只更新并重启tushare-worker；文档、quantmind和普通worker不重启。候选4115b14（e0b002b等价），联合520 tests23.167秒通过，schema6/任务/历史游标不变。先仅取消tushare_acquire消费并自然排空。Mac新版压缩清单mirror正在独立运行，读取固定版本不受消费暂停影响。

恢复后text只读确认真实history attempts、旧签名和gates；root完成本批Mac固定版、台账/文档与耐久证据。保持全263+13目录范围，不能把候选/入队数说成完整历史。
