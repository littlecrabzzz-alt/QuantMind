# Incremental document index v2 ready
- /root/structured_contracts, Mac isolated worktree codex/tushare-structured; commit60d82737548f079ec4230b06bf1c1c9793313b70. Own only tushare_documents.py, routers/tushare_data.py, new scripts/test_tushare_document_index.py. Parent owns pipeline/mirror/Agent and production publication.
- New document_index(root) returns {path,sha256,bytes,mime,schema_version:2,files:[originals/extracted+current shards+index],counts,reference_counts,totals,rebuilt_shards}. Parent must merge all returned files into release manifest and store documents={path,counts,reference_counts}; never expand index into the manifest. Existing document_inventory remains unchanged v1 compatibility function.
- refs/attempts/files use stable1000rowid ranges; latest document state uses doc_id first2hex buckets. Cached content-addressed documents/*.json shards retain unmodified ranges; metadata schema document_index_meta(version1) and dirty triggers live in existing documents.sqlite without changing queue user_version2. Old running writer connections mark dirty automatically. First build uses a consistent SQLite transaction; failed build rolls back cache/dirty changes, immutable partial evidence is harmless and reused next time. No old file deleted.
- API supports v1/v2 and new view=attempts. v2 small-page reads only needed reference/attempt/file shards and matching state buckets, restores legacy mapping latest_result/validation_observation fields. Each accessed index/shard requires manifest membership, exact sha/bytes, safe hash paths and symlink rejection; HTML/text behavior unchanged.
- Validation:6new tests +16document tests +6readonly API +6Agent integration passed, Ruff owned files and diff check passed.12,035refs cross-boundary5row page reads4objects(index+2refs+1state), out-of-range readsindexonly. Append reuses full shards; retry changes1statebucket+attempttail while other bucket unchanged; old release state/time preserved; v1, orphan originals, rollback and malicious paths/hash/symlink checked.
- Synthetic Mac benchmark:130,000refs all sharing1document, first build2.197s/tracemalloc peak1.70MiB,46,989,235indexedbytes. Append100refs adds only1tail shard+index:67,822bytes, index31,559bytes. This measures reference-copy elimination, not cloud RSS or diverse-state timing. Initial build briefly holds SQLite writer lock; future state buckets scale with documents/256; original-file metadata scan and small index list still grow, but full refs/attempts are no longer recopied each publish.
- No production calls/writes, secrets, service restart or deployment. Parent must pair publisher switch with returned shard file inventory before claiming live v2 availability.
