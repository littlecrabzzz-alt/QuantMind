# Document registration budget candidate ready

- Owner: structured; branch `codex/tushare-document-registration-budget`, candidate `30894be`, base `0c18fb2`. Files released after this handoff. No production/upstream operations, no config/resource/quota changes.
- Reported exact failure: task `8c6a1bc0-9b58-4387-ba85-3ff02cde72fb`, 160-second soft timeout, `tick -> register_documents -> enqueue_documents` SQLite context. Trace alone cannot identify BEGIN/body/COMMIT wait. Code confirms unbounded whole-observation row processing plus ten-second per-open busy timeout; previous deferred identifier split does not bound this stage.
- Default registration: five-second cooperative deadline, 500 source rows, chunks <=100; contiguous attempt scan <=1,000. SQLite busy timeout <=50ms and interruptible SQL, restored pipeline timeout. Existing schemas unchanged; partial offsets stored in existing scheduler_state, keyed to exact attempt/result/attachment fields. Direct legacy enqueue calls retain old interface/semantics.
- Crash order: document transaction COMMIT first, then pipeline row-offset/whole-observation cursor COMMIT. Document failure rolls back chunk and reports zero progress. Cursor failure after document commit leaves original checkpoint and deterministic reference IDs permit idempotent replay. Busy/deadline returns deferred; unexpected integrity/source identity problems fail closed. Source raw/observation/attempt rows unchanged, no failed or partial row silently skipped.
- Python3.10 verification: 11 dedicated tests, included in 62 document tests (3.580s); 173 pipeline tests (15.071s); 43 archive/timing/interval tests (0.726s). All passed; Ruff and git diff --check passed. Dedicated cases use real isolated SQLite BEGIN/COMMIT/checkpoint locks, costly SQL interruption, 1,205-row three-reopen legacy parity, crash/source-change/empty/reuse/unrelated-tail behavior. Two old fixtures updated for additive report/committed-progress receipt.
- Local isolated report `/tmp/tushare-document-registration-budget-report.json`, SHA256 `04d62c330efbc77aeb89c0eeda5ed923b62d0e606c5d161c23faf03cc64ff776`. Replay `[500,500,205]`; real document writer lock returned deferred in 0.0653s, resumed after unlock. These are fixture measurements, not production improvement claims. Reproduce via `/tmp/measure_tushare_document_registration.py`; repo commands and constraints are in `docs/tushare-document-registration-budget.md`.
- Limit: one original JSON read/decode, a record's Python hashing and fsync are not OS-preemptible; no claim of absolute five-second execution under arbitrary IO stalls or complete resolution of every timeout. Registration backlog can postpone attachment availability without dropping its durable obligation. No whole-library production audit performed.
