# Mac Tushare 权限重验证逻辑开始

- 时间、节点：2026-09-18T05:51:00Z，Mac 全量归档所有者。
- 实时状态：归档 worker PID 68745 持续运行；结构化队列约 286.9 万 pending，最近完整轮约 685 请求/104 秒；文档下载和 PDF 解析并行继续。
- 审计：capability 台账有 210 个 available、34 个 permission_denied；当前已购买的公告、新闻、董秘问答、政策法规、券商研报和货币政策接口均已真实识别为 available。拒绝项主要是未购买的分钟/实时、港美股财务和独立可转债权限。
- 缺口：`permission_denied` 会把同权限域任务永久转为 `permission_blocked`。账号以后购买独立权限或积分档变化时，现有逻辑没有自动重验证和恢复路径，可能遗漏后来可用的数据。
- 实施边界：隔离 worktree 中增加低频、有上限的代表任务重验证；保留原结果、tries 和 attempts。代表请求仍走生产共享限速。只有真实返回可用结果后才把同 API/src 的其余 `permission_blocked` 任务恢复为 pending；再次无权限则保持冻结并刷新真实证据。不得批量盲重试或假定权限。
- 验收：无网络单测覆盖到期选择、幂等、上限、证据保留、重复拒绝与成功后精确恢复；随后在周期边界部署并观察真实 stale scope 探针、账户频控、任务恢复和普通吞吐。
