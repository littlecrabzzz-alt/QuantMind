# Document registration budget — start

Root assigned exact160s failure task8c6a1bc0 trace tick→register_documents→enqueue_documents(with db). Branch codex/tushare-document-registration-budget from0c18fb2. Own register_documents, documents._document_db/enqueue_documents directly related budget handling, dedicatedtest/doc and minimum oldfixture expectations. No downloads/claim/publish/queue/rate/resource/config changes. Whole observation registration is currently unbounded (20 observations, each records×fields single transaction; eachSQLite busy wait10s). Exacttrace without detailedSQLframe cannot distinguish body fromcommit wait. Candidate uses short SQL waits, deadline/cooperative bounded chunks and persistent per-attempt row offsets; doccommit precedes pipeline progress, crash replay uses existing deterministic reference IDs. No production/upstream operation.
