# Tushare 文档登记积压开始净下降
- 时间、节点、任务标识：2026-09-18，Mac，document-registration-batch
- 状态：生产保留；本地全量采集与附件下载继续运行
- 分支、提交：`master` `b2cc6a79`、`a7441997`；候选分支 `codex/tushare-document-registration-batch`
- 分工：本次只调整登记批量上限、截止时间及对应测试；没有修改数据身份、供应商限速、附件下载边界或云端全量写入规则
- 接续：`20260918T110940Z-mac-document-registration-batch-start.md`

候选的批量复用查询没有保留：同一份生产库 APFS 克隆、同一 6000 行 `anns_d` 输入的三次交错中位数仅由约 5.997 秒降至 5.793 秒，约 3.4%，源码已恢复，未部署。保留的最小代码只把有界登记上限扩到 200000 行、90 秒；每个 SQLite 事务仍最多 1000 行，游标、精确 offset、幂等引用及失败回放语义不变。私有配置最终为 200000 行/90 秒，SHA-256 `9c5783305de8e22065eefefd9f9ff575645bbd1fe37c7e14081a2dcc0662aca5`，权限 0600；未记录凭据。

验证：13 个登记专项测试通过，Ruff、编译和 `git diff --check` 通过。更宽的 96 项关联集合与基线同样出现既有的 planning 计数、MagicMock JSON 及本机依赖问题，因此不归因于本次两行边界变更。先以 20 秒生产得到 36000 行/6 个观测；50 秒三轮均为 100000 行，17/17/16 个观测，但同期附件积压由 10839 增至 10849，未保留该档。最终 90 秒三轮分别登记 177945/9800/200000 行、完成 45/17/37 个附件观测、扫描 1000/1000/592 个连续 attempt；供应商真实请求为 555/620/592，总周期 174.860/108.564/177.765 秒，三个 `failed_stage` 均为空。

最终登记游标由 177592 前进到 180200；期间供应商 attempt 最大 rowid 由 696076 增到 698357，原始 attempt 差距由 518484 降到 518157。待登记附件观测由 10849 降到 10798，已开始净回收；当前分项为 `anns_d=6915`、`idx_anns=1394`、`research_report=1185`、`npr=696`、`monetary_policy=608`。服务 PID 57545 运行，配置与 ENABLED 均为 0600，可用磁盘约 2.15 TiB；最新文档轮处理 1558 个阶段（下载 793、解析 765）。

下一步：保持该灰度继续观察积压、下载队列、磁盘和发布；云端只同步代码及受限研究缓存，不恢复供应商采集或全量数据库。后续统计以连续游标和真实待登记数为准，不能用单轮附件观测数替代完整性。
