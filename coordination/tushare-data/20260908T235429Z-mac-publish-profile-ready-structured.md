# Read-only publish profile handoff

Evidence `/tmp/tushare-publish-profile/{report.json,fixture-report.json,expected-files-profile.txt}`. Repeat `/tmp/profile_tushare_publish_readonly.py` and `/tmp/profile_tushare_publish_fixture.py` with `uv run --no-project --python 3.10 python`. Exact read-only a972ed1 functions extracted from committed AST; verified local fixed release05a74924173402344b86b4bd515828aacd7b373c9d6d969673025c97b8290f03. No upstream/production calls/writable production DB copy. Temp SQLite populated from immutable manifest summary, not live database.

- Fixed manifest61.55MB/137526files; files23.10MB, partition_closure22.67MB (1161parents/328768children), datasets6.97MB, gaps5.94MB, planning2.69MB. Therefore schema3 index noop0.578s cannot explain whole publish12-25s. Full manifest still writes~61.6MB per changed tick; at one new release each120s unchanged size would be~44GB/day of manifest bytes (hypothetical rate, not measured daily growth; archived aliases are hardlinks).
- Python3.10 Mac: read0.005s/SHA0.018s/JSONdecode0.132s; full manifest_at0.212s; retain _expected_files0.238s; JSONencode0.195s; metadata16,090files enumeration0.123s;3,000sample pathstats0.0095s warm. These are local isolated operation timings, cannot attribute all cloud variance. _expected_files cProfile mainly metadata regex/path construction, not blob hashing.
- Exact partition_inventory on isolated manifest-derived fixture0.061s. Synthetic566134jobs/91.8MBrepresentative jobJSON confirms current coverage query table scan+TEMP GROUP and scope epoch/APIindex scan+TEMP DISTINCT;0.249s/0.195s. Optional api/state expression index shrinks both to~0.02s/~0.013s for~14.8MiB index, unchanged grouped data. This is only a future candidate, no runtime/schema edits and no production seconds claim.
- Most important unchecked production blocks: everypublish rereads ALL attempts JSON plus2/3stat perattempt (pipeline2015..2047); archive_inventory allverifiedrows/datasetJSON twice (2061/2219); sameCURRENT read/hash/decode again in retain_release->_manifest_document (archive410..422), full expected metadata validation and manifest alias verification; coverage/closure construction2100..2140; final serialize and61MB atomic write/fsync2226..2234. These need native substage timings before selecting optimizations. No full object-content hash loop in publish itself; mirror verify is a separate full-file operation.
- Minimum real observation needed is publish block walltimes plus attempt_count/SUM length(result), jobcount and archived verifiedfile count; no upstream/diskdu/full source bodies. Parent now authorizes a separate timing-only candidate, so this read-only diagnosis concludes without claiming a bottleneck speedup or implementing index migration.
