# Schema3 two-level document states — isolated candidate ready

Owner text_contracts. CUMULATIVE delivery commit 9cf3a60d90288f3ee41a0e7128bcee0fdceed396, parent c62cb54, branch codex/tushare-schema3-candidate-20260909. Pick this ONE commit after the manifest alias base; do not separately deploy5c521f1 flat3hex. Candidate tree's4 owned files verified byte-identical to tested codex/tushare-text HEAD0fa2e5b; c23d9f9 benchmark script intentionally excluded from runtime candidate. No archive/mirror-alias changes, production operations, old-file deletion or in-place format rewrite.

Implementation: schema3 top index has <=16 firsthex descriptor references, state_prefix_chars3 and state_descriptor_prefix_chars1. Intermediate descriptors and state leaves reuse immutable schema2 kind/bucket/items format. SQLite document_index_meta migrates1→3 (two-prefix triggers/cache) or2→3 (previous flat3hex), transactionally; mappings/attempts/files rowid1000 chunks unchanged. Only changed firsthex descriptor groups re-encode; unchanged groups reuse cached content-addressed descriptors. Original v1/v2 indexes and all shards survive. Parent existing publish files closure automatically receives current descriptors/leaves, inherits and retains historical files. A new Mac needs only CURRENT+its verified file inventory, no SQLite.

Reader: schema1 direct inventory and schema2 oldflat indexes supported; schema2 absent prefix defaults2. Schema3 follows two levels with validated prefix/count/state-count metadata, descriptor/leaf path/hash/bytes membership, loaded payload checksum/kind/bucket/count, unique correct-prefix state IDs and mandatory matching states for each document reference. Result source files must match the release inventory; actual source bytes continue to be checked by artifact readers/mirror, not reread as full PDF bodies on every metadata page. Missing or wrong references explicitly fail instead of being returned as empty state. Checks remain page-scoped for loaded descriptor/state bodies; root references and every leaf reference inside each loaded descriptor block are validated. Old reader at c62cb54 explicitly rejects any schema other than1/2 with409, thus schema3 cannot silently partially render.

Tests: document_index6, document_buckets5, pipeline17, documents16 passed (44), plus final data_api regression recorded separately by agent. Ruff/diff checks passed. Covers current and historical fixed indexes, CURRENT-only fresh client reading archived old release and HTML text; no-op; dirty/migration/descriptor-save interruption and retry; missing/tampered descriptor/leaf/source membership; wrong state identity; immutable originals retained. All use temporary directories, no credentials/upstream.

Actual-function synthetic write benchmark: /tmp/quantmind-document-schema3-actual-writes-20260909.json. Generated316831 documents+362139 refs, old c62cb54 document_index vs candidate document_index; every new metadata file counted including leaf+descriptor+root. Initial metadata old296527023B/620files vs new297659771B/4476files. Noop0B for both. Single-state680169→180487B (-73.5%);30-state14427409→1859157B (-87.1%);1000new130949866→29500052B (-77.5%). New root90027B; changed intermediate groups1/14/16. Mutations sequential within each isolated layout, simple pending-state fixtures; not production byte/latency guarantee and excludes SQLite growth/filesystem allocation. Known ceiling: more initial files/inodes; large changes can dirty most leaves/all16 descriptor groups; root's mapping-descriptor list continues growing, full historical release manifests remain full inventories. No additional index framework added.

All owned runtime/test paths released. Parent independently reviews candidate and selects a later controlled deployment; no deployment performed here.

Final API regression6/6 passed, including schema1 document pages/text; total50 focused tests passed.
