# Index weight descendant batch 2

- The optimized read-only gate froze 180 complete sibling pairs / 360 pristine `index_weight` leaves against fixed release `data-77bb1b4c…`. Authority inspection fell from roughly 557 seconds to 15.902 seconds after replacing 360 unindexed logical-key scans with one any-epoch batched query; a direct production benchmark of the query took 2.982 seconds. Commits `e540b05e` and `53ba7a9e` preserve the cross-epoch exclusion rule and pass 23 targeted tests.
- The exact production window completed 360 HTTP-200 calls in 45.865 seconds: 180 empty, 180 split pending, 285,105 retained rows and 360 pristine direct grandchildren. The graph moved from 540 tasks / 360 edges to 900 tasks / 720 edges.
- Independent read-only closure checked each job, its single attempt and result chain, then verified 900 unique object/observation/Parquet references and 26,188,287 bytes by SHA256. Six artifacts were archived with create-only semantics under `validation/index-weight-descendant-batch-20260912/batch-2-*`.
- A first closure helper invocation failed before output or authority writes because its file-path regex was over-escaped; the corrected read-only invocation passed. Worker and Beat were restored using their existing containers and are healthy with restart count zero and no OOM.
- This batch did not publish or switch `CURRENT`. Normal fixed publication, Mac mirroring and local exact verification are pending. Deeper descendants, empty-result confirmation, complete history, revisions, `known_at`, PIT semantics and RRG classification remain open.
