# Acquire-only timeout candidate — start

Structured branch codex/tushare-acquire-timeout-review, baseline4446d36. Latest AGENTS/cadence acceptance read. Own pipeline.run plus directly related deferred identifier-split helper, dedicatedtest/doc; no identifiers/cache/planning-policy edits. Remaining owns identifiers observed stk_rewards pair additions. Root authorized deferred expensive fanout design: persist raw/observation/Parquet/attempt first, next run resumes at most one durable existing parent without HTTP and returns; fastdate/futures split unchanged. No schema/config/production/upstream actions. Supplied acquire157.201s timeout lacks method traceback (live-samples failure slot hasnull stagefields), so exact production trigger remains inference; isolated tests will establish whether the actual code path can exceed the budget and whether deferral preserves evidence.
