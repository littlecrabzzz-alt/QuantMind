# Tushare：当前范围与终态队列审计

- 时间、节点、任务标识：2026-09-17T21:23:32Z，Mac，scope-terminal-audit
- 固定版：`data-6466fd66bd816f503e0887b226a05be8fbe1095a585ba1d5404fa2b52723ac21`
- 边界：离线读取当前固定版、合同与生产 SQLite；上游调用 0，未修改生产数据或队列。

范围审计：263 个基线页面、25 个发现页面，共 248 个目录/发现命名接口，另有 2 个命名相邻义务，总命名范围 250。247 个接口已注册且全部有固定版 reader；范围内未注册的 4 项为失效正文且真实接口名拒绝的 `ggt_monthly`、永久排除自动化的写操作 `p_save`/`p_delete`、SDK 派生包装 `pro_bar`。注册范围外的 `fina_mainbz_vip` 是已登记的正式财务别名实现，不是新目录缺口。

固定版审计：247 个已注册接口中 246 个已有持久规划证据，197 个已有发布数据集。唯一未规划的 `p_get` 依赖账户私有组合列表；当前 `p_list` 只有来源空结果，不能猜测私有组合 ID。其余 49 个无发布数据集接口全部有显式终态证据：37 个权限拒绝、11 个供应商接口错误、1 个来源空结果；逐接口查询生产队列后，49 项的 `pending` 与 `split_pending` 均为 0。

生产 `blocked=874` 的构成：`dc_member` 621 项和 `moneyflow_dc` 142 项已由股票代码范围计划替代，`index_weekly` 1 项已由指数周期计划替代，三类父任务的替代 gap 均保留且活动子任务为 0。`index_weight` 91 项已缩至单指数单日仍达到或超过来源上限；`bc_otcqt`、`kpl_concept_cons`、`share_float`、`tdx_member` 各 1 项已到合同允许的最小分区仍饱和。11 个旧接口保留供应商 `40101` 接口名错误；`stk_mins` 2 项保留 1 次/分钟与 2 次/天的实际试用限额证据且自动采集关闭。

`npr` 两个 500 行终态进一步用已保存原文复核：两份响应的全部行都集中在精确时间 `2008-03-28 08:00:00`。无机构请求包含 4 个实际机构，机构 `国务院` 过滤后仍返回 500 行；已验证的 `ptype` 输入对文档值返回空。供应商未提供分页，继续拆时间会重复同一秒，猜测其他筛选维度不构成完整性证据，因此保留显式饱和 gap。

结论：当前命名范围、注册、reader、权限终态、失效接口和合法最小分区之间没有发现可执行遗漏。此结论证明数据逻辑闭合，不证明 288 万级历史队列已经下载完成；Mac 唯一写入者继续运行全量本地采集，固定版将在正常六小时发布周期纳入新增数据。
