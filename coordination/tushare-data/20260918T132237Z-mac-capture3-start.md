# Mac Tushare 三路隔离采集候选开始

- 时间、节点、任务：2026-09-18T13:22:37Z，Mac，capture3
- 状态：开始有界代码调整，生产配置尚未改变。
- 范围：只将 `acquisition_pipeline_depth` 和 `acquisition_capture_workers` 的候选上限从 2 提高到 3；三路仍必须使用 process 隔离，且 worker 不得超过 pipeline depth。不改数据合同、请求身份、共享账户/API 频控事务、每日配额或云端角色。

最近生产轮在 100 秒采集预算内完成 396–494 次真实请求。其中捕获时间累计约 68–78 秒，双路后的理论墙钟约 34–39 秒；主进程选择/频控和结果提交合计约 52–64 秒。账户 gate 实际等待只有 32–49 秒，仍明显低于 500 次/分钟的既有账户上限，因此存在可量化的网络/落盘重叠余量。

候选需用本地 HTTP 夹具证明峰值三路、三个 observation 全部持久化，并拒绝超界值、thread+3 和 worker 超过 depth 的组合。生产继续保持 `batch_requests=800`、`batch_seconds=100`、账户 500 次/分钟及全部接口级限制，至少观察三轮真实请求、供应商错误分类、失败阶段和资源占用；无收益或异常则恢复双路。
