# API HTTP evidence ready
- structured_contracts; candidate `2bf6681abc5ba0f56f759c7885e26e0673bb9d18`, based on merged8249f6c. Only tushare_intake.capture_sample + new test_tushare_http_evidence.py; AST confirms assess_response and every other function unchanged. No production requests or writes. Ownership released to parent integration.
- Complete HTTP429/500/401 JSON or HTML response bodies now pass through existing literal Token defensive redaction and immutable objects/<sha>.json/observations/<uuid>.json. HTTP429 remains rate_limited; other HTTP failures transport_error. A code0/data body under HTTP error is not assessed/normalized as successful data. Result and observation assessment include http_status,response_complete,response_format(json/non_json). Same body dedupes objects while observation/status/times remain per request.
- ReadTimeout, ReadError after partial stream, and early response hook without read body create no fake complete body; status includes response_complete=false. Already-read response hook HTTPStatusError still retains available body/classification. Exception messages/response request bodies/token fields not serialized. Existing corrupt object is rejected, never overwritten.
-7 new tests; 54 HTTP/intake/pipeline/partition/equity-event/observed-fanout checks passed, Ruff/diff clean. Fixtures MockTransport only. Probe verify_release and fixed-release verify_data accept these raw-object hashes/paths.
- Parent notified: .json raw object naming does not guarantee JSON body (also true for pre-existing HTTP200 invalid_response). Pipeline.records and old intake.read_samples need safe HTTP error/nonJSON handling; parent owns these, do not drop evidence or expose HTML as executable data. Fixed-release analytical store remains Parquet-only. No changes to mirror path format required.
- Continues 20260908T204322Z-mac-http-evidence-start-structured.md. Later work may interpret reviewed API-specific HTTP429 bodies; this candidate preserves original HTTP classification and adds evidence only.
