# Mac recent 队列去重与文档预算已生产验收

- 时间、节点、任务标识：2026-09-16T22:04:07Z，Mac，Tushare 本地全量归档持续同步。
- 状态：生产代码与数据迁移已验收；历史、文档及权限缺口继续补采。
- 分支、提交：master `9931d6d5`；接续 `20260916T211236Z-mac-periodic-planning-cache-hit.md`。

代码只复用仍未执行的非 `history` 同逻辑 recent 根任务；每日压缩旧根及其私有分区树，已完成响应、原始对象、稳定 history 图和共享叶子保持不变。发布清单保留 `superseded` 总量，但不把它误报为数据缺口。Tushare 官方当前文档确认 `opt_daily` 可按代码、日期或交易所查询，`moneyflow_dc` 可按日期或股票，`dc_member` 可按板块、成分股和日期，`factor_value` 必须至少提供股票或因子名；后续分区优化仍以真实触顶证据为门槛。

验证：52 个规划、队列、发布、分区闭包和 worker 测试通过，另有既存 ResourceWarning。生产数据库写时复制副本演练用 132.593 秒归类 2,642,954 个旧任务，完整 `integrity_check=ok`；副本随即删除。真实库在三把写锁下用 123.707 秒归类 2,643,940 个旧任务，pending 从 7,837,243 降至 5,193,687，done/empty/quality/permission_blocked 未变，活动父任务指向 superseded 子任务为 0，重复活动 recent 根为 0，`quick_check=ok`，可用空间约 2.36 TiB。运行时代码 SHA-256 与仓库一致，新进程 PID 59099 首轮 72.693 秒完成 360 次上游调用和 240 个文档阶段（120 下载、120 解析），无失败阶段。

下一步：观察下一次周期规划返回 `already_compacted` 并确认不再新增跨 epoch 重复根；基于官方交易所输入和生产触顶证据评估 `opt_daily` 先按交易所再按合约的合法分区，继续消化剩余约 519 万请求及约 136 万文档任务。全量历史尚未完成。
