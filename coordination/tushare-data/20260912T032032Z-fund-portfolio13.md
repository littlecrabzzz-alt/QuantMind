# Tushare fund_portfolio batch 13 authority closure

- Beat was stopped first and the active ordinary acquisition completed under a warm Worker shutdown; no task was revoked. The production preparer then reproduced the audited candidate hashes exactly and froze 240 pristine `current_v2` history leaves from 11,608 eligible semantic requests.
- Plan-only accessed no credentials or upstream and made no data/publish writes. The exact production-container run completed 240 HTTP 200 calls in 66.574 seconds: 122 done, 118 empty, 4,912 rows, zero uncertain calls and 216.301 rpm under the conservative 240 rpm API gate.
- Independent read-only audit matched every live job, attempt, result and observation chain. All 602 unique physical references (240 object, 240 observation, 122 Parquet), 1,568,175 bytes, passed size and SHA256 verification; task IDs and request signatures have zero overlap with batches 7-12.
- Worker and Beat returned healthy with restart count 0 and OOM false; normal acquisition resumed. Cloud storage retains 411,031,371,776 bytes above the 100 GiB reserve.
- The first preparation path was correctly rejected because it was inside authority; its zero-byte stdout placeholder was validated and removed, then the same candidate was prepared through a separate temporary mount. No lock, credential, upstream request or authority data write occurred in the rejected attempt.
- `CURRENT` remains fixed release `data-a21e55cc…`; batch13 has not been published or mirrored to the Mac yet.

This batch expands periodic disclosed holdings. It does not establish complete holdings, daily PCF, revision history, intraday `known_at`, PIT membership or authoritative ETF-industry mapping. Machine evidence: `docs/tushare-fund-portfolio-batch13-20260912.evidence.json`.
