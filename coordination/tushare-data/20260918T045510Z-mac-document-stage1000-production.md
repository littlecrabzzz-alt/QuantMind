# Mac Tushare 文档阶段上限 1000 生产验收

- 时间、节点、任务：2026-09-18T04:55:10Z，Mac 全量归档所有者，document-stage1000。
- 配置：私有 `pipeline-config.json` 仅将 `document_worker_max_documents` 从 600 改为 1000；旧哈希 `87312dec4e74621fa173357702432ef30d72298840a5dfecd30dbd09f2d58c99`，新哈希 `e2b4a31b57822dfd315b24770c12e5c85fd96c219628ff561acaeb9ec45645fa`，模式保持 0600。100 秒总预算、四路下载、20 秒单下载、失败退避、来源保护和 API 限速未变。
- 生效边界：修改配置时已开始的旧值周期仍完整结束于 600 阶段、736 次 API、304 下载、296 解析。下一轮配置指纹规划周期完成 657 阶段、336 下载、321 解析且无失败，证明新值生效；该轮结构 API 为 0，不纳入 API 比较。

真实生产验收：

- 新值三轮分别为 765/657/336/321、758/625/320/305、740/497/256/241（API 请求/文档阶段/下载/解析），每轮登记 5000 条，文档任务均受 100 秒时间预算结束，`failed_stage=null`。
- 三轮平均 API 754.33、文档阶段 593、下载 304、解析 289。紧邻调整前的三轮均准确碰到 600 阶段，平均 API 740、下载 304、解析 296；因此新值没有提高这三轮平均下载量，但消除了快周期的 600 硬截断，两轮分别额外完成 57 和 25 个阶段，慢周期仍由来源尾延迟限制。
- `source_challenge` 保持 5275，`download_timeout` 保持 81；retry 从调整前边界 70 变为 76，伴随新增下载尝试但未形成挑战或周期失败。PID 40497 仍只启动一次且未退出。

验收边界结构化 done 305587、empty 264775、pending 2876570；附件 pending 1551415、parse_pending 77604；可用空间 2475329323008 bytes。保留 1000 上限用于快周期的剩余时间，不把它作为稳定吞吐提升；下一瓶颈仍是有界下载批次的尾延迟。Mac 继续唯一持有和写入全量归档。
