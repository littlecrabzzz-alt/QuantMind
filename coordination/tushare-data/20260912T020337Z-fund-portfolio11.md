# Tushare fund_portfolio batch 11 authority closure

- After a no-revoke drain, the exact runner froze 240 pristine `current_v2` history leaves for 240 unique funds. The manifest fixed the b0c6 source release, task and request-signature sets, config, preparer and helper hashes; plan-only accessed no authority or credentials, made zero upstream calls and performed no writes or publication.
- The real window made 240 HTTP 200 calls in 63.583 seconds (226.476 rpm under the conservative 240 rpm API gate): 144 done, 96 empty, 8,328 rows and zero uncertain calls.
- Independent read-only audit checked live SQLite and every physical file: each task has exactly one attempt, and 624 unique references (240 object, 240 observation, 144 Parquet), 2,124,838 bytes, have zero missing, SHA or Parquet-size errors. Task and signature overlap with batches 7-10 is zero.
- Five archive files initially landed in the prior date directory. They were moved by exact filename into the empty `validation/fund-portfolio-batch-20260912` directory without rewriting data, then their sizes, hashes, identities and cross-file links were independently reverified.
- Before the first restore, two queued messages were proven to be expired `engine.tasks.tushare_acquire` messages. The worker discarded them as revoked/expired; queue length returned to zero. A second drain enabled full live audit, then Worker and Beat returned healthy with restart count 0 and OOM false; normal task `71c1256b-9fe3-4639-b2fa-007caa898b7a` was received.
- `CURRENT` remains `data-b0c60c…`; batch11 is not yet published or present in the Mac fixed mirror. Cloud free space is 519,417,618,432 bytes, leaving 412,043,436,032 bytes above the 100 GiB reserve.
- Evidence commit `90616b97` was pushed to GitHub master and aligned to the cloud working tree with matching source digest. The cloud `origin/master` tracking ref stayed stale after a bounded fetch timeout; this is recorded as non-blocking because cloud HEAD and deployed files are exact, while Mac `origin/master` verifies the push.

The batch expands periodic disclosed fund holdings available for a later RRG exposure product. It does not establish complete holdings, daily PCF, revision history, intraday `known_at`, PIT membership or an authoritative ETF-industry mapping. Machine evidence: `docs/tushare-fund-portfolio-batch11-20260912.evidence.json`.
