# Bounded read-only throughput / parallel review

Evidence: /tmp/tushare-throughput-review-20260908T232305Z.json. Main1fba0bf code, sanitized live status + read-only SQLite LIMIT100 latest document attempts inside existing cloud-compose business containers. No API fetch/config/runtime/restart/full-disk scan. Snapshot untouched; parent later reports backup exit75 and candidate d6cc1fe, not independently operated here.

## Measured bottlenecks

- API:328requests/90.332s=218/min active (91% of local account gate240/min). Whole tick117.602s: initialize3.685/planning6.468/archive2.591/docregister0.695/publish13.744. Current120s beat means about164/min sustained if this sample repeats, not240. Generic interface gate is min(config default200, contract rpm), plus quota-specific delays; hk_daily config3605s cannot be accelerated by extra workers. All requests reserve shared account gate first and pipeline.lock preserves single writer. Extra normal workers cannot safely bypass this.
- Docs: already separate Celery queue/consumer,2download subprocess slots,1parser,90s/100stage ceiling,20s download deadline. Still no overlap between download-pair and parser; pair pool exits only after both futures, so fast lane can wait for slow lane before replacement. Last100phases over404.4s:76download attempts,23success,37dfcfw pdf_content_mismatch,16cninfo timeout;24parse attempts with22parsed. Later sample confirms31mismatches are HTTP200/application-pdf, raw_complete unknown bytes (examples984/989bytes), preserved asbin. Diagnosing these existing bytes is more useful than multiplying the same failing downloads.351140pending and1049parsed. Recent success rate~3.4/min implies roughly71days for static backlog, deliberately not ETA: new refs/retries/host mixtures change it. API progress alone obscures this bottleneck.
- Mac: lastverified23:09:58Z/119229files/4300downloaded; cloud23:18:44Z newer. Around11min lag at audit, but mirror has no elapsed timing, so cannot claim transfer duration. Manifest/files verification loops all retained objects; schema3 candidate reduces state metadata rewrites, not all-file verification complexity.
- Resource snapshots: API686MiB/1GiB, docs477MiB/1.5GiB. No OOM evidence. Docs cgroup CPU usage grew3.43s over~68.8s between snapshots, so no evidence that doubling CPU alone cures current waiting; CPU throttling cumulative counters cannot prove current bottleneck. Backup IO may confound timing; do not attribute disk/free space to Tushare alone.

## Minimal recommended parallel work

1. Parent integrates already-tested schema3/field guard then compares3-5normal ticks' acquire/publish/total timing after backup has settled. Keep gates and bounds fixed to measure improvement; avoid stacking new API workers.13.7s publication is real remaining overhead.
2. Independent attachment investigation using saved errorbin/header evidence and bounded per-host outcome/stage timing; then candidate continuous refill of existing2download slots, at most1parser, explicit fairness/claim/deadline/locks and same resource budget. Offline fast+slow fixture first, bounded production comparison later. This addresses pair waiting without unbounded threads; do not silently parse mismatched bytes asPDF or retry aggressively.
3. RRG fixed-release exact code/date/warmup/PIT validation and offline API/Agent work can proceed while full catalog history continues. RRG weight3 of15 gives about20% share when all families ready, not exclusive service or proof of readiness. Pending RRG examples are1990calendar/CI and2023fund portfolios: aggregate pending463704 is not the RRG readiness gate. Prioritize only verified missing slice jobs; retain other family weights/history jobs and explicit gaps, never cancel whole coverage to manufacture completion.

No runtime commit; audit complete and ownership remains unclaimed.
