# Discovery caching bounded read-only diagnosis
- Mac remaining_markets；只读当前79f4498源码/既有生产timing；独占/tmp/tushare-discovery-cache-audit/设计、fixture、报告。无runtime改动、云端Pipeline实例、源请求/队列/生产写入。
- 审查attempts/objects/observations不可变边界、rowid/rollback/换DB；优先复用全metadata扫描+派生投影memo，拒绝未经证明的仅max(rowid)增量前沿。
- 父正发布，本文只交证据与最小下一步，不承诺整体吞吐收益。
