# Tushare index_daily exact batch 21

- A controlled window stopped Beat first and allowed the ordinary Tushare worker to exit with code zero. The waiting command's stdout was unavailable, so no drain duration or task ID is claimed. Docker independently showed both containers exited before execution; the main service, document worker and QuantDB remained running.
- The exact preparer pinned fixed release `data-8b3e0b4a4937bb57830be68272ea19d4fa812310561e7a58b3eddb039b71d2c7` and froze 360 pristine `index_daily` history tasks. All were pending with tries zero. The selected suffixes were CSI 161, SZ 198 and BJ 1; `.CFX`, `.SI` and `.SW` were absent.
- Independent admission covered 20 prior manifests and 6,900 historical tasks. Task ID, logical key, canonical API/params request and full-job sets were unique and had zero overlap with batch 21. Plan-only verified all 360 records and all five `would_*` fields were false.
- The first host-path execution was rejected by the authority-root guard before credential or upstream access because production requires `/data/tushare`. The accepted run used the production container and read every pin from the accepted plan.
- The exact run made 360 HTTP requests in 50.309 seconds: 286 done, 74 empty, 67,735 retained rows. Observed throughput was 429.347 rpm under the 500 rpm API/account gates and the 90-second hard deadline.
- Independent closure verified each task's single attempt/result chain, raw response, observation and Parquet row count. It then rehashed 1,006 physical references totaling 15,520,675 bytes: 360 objects, 360 observations and 286 Parquet files; errors were zero.
- Manifest, prepare, plan, overlap audit, receipt, inventory, closure, physical audit and audit helper are create-only under `validation/index-daily-batch-20260912/batch-21-*`. Worker and Beat were restarted as the same containers; all services are running with restart count zero and no OOM. QuantDB was never stopped.

The batch remains authority-only until a normal fixed publication, cloud verification, standard Mac mirror, Mac exact verification and offline read. Empty results, complete index history/universe, revisions, known_at/PIT and RRG claims remain open. Machine evidence: `docs/tushare-index-daily21-20260912.evidence.json`.
