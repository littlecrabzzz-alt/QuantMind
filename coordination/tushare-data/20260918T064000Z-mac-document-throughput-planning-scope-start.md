# Mac Tushare 文档吞吐与规划指纹修正开始

- 时间、节点：2026-09-18T06:40:00Z，Mac 完整归档所有者；生产归档 worker 继续运行。
- 基线：最近完整周期结构化采集 643 请求/100.062 秒、`failed_stage=null`；文档处理 145 阶段，其中下载 80、解析 65。文档有 1,698,128 pending、86,154 parse_pending、44,925 parsed、7,365 blocked。
- 发现：规划周期目前哈希整份运行配置，因此仅修改文档下载/恢复参数也会触发一次 `planning_only`。这不会写模拟数据，但会浪费一个结构化采集周期并造成“演练”误解。文档下载上限还被代码固定为 4 worker，当前下载槽位有明显空闲且 169.8 万存量是主要长期瓶颈。
- 实施：在隔离 worktree 让规划指纹只排除明确的文档执行参数，保留所有采集/目录/权限配置变化触发规划；兼容并原子迁移当前旧版整配置指纹，不额外触发规划。把文档下载并发的校验上限扩至 8，先灰度到 6，不改变解析串行、总时限、单请求时限、磁盘保护或终态冷却。
- 验收：离线覆盖规划相关配置仍重规划、文档配置不重规划、旧指纹迁移、并发边界；自然周期部署后先保持 4 worker 验证代码，再只改文档配置到 6，确认结构化采集未进入 `planning_only`，连续真实周期无失败并量化下载吞吐、超时/HTTP/挑战状态和资源占用。
