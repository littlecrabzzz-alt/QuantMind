# Tushare top10 holders exact batch 2

- 正式 preparer 在共享锁内固定 `data-960e25bc…`，选出180个完整 `top10_holders`/`top10_floatholders` 兄弟对、共360个pristine history任务；两接口各180，SH/SZ/BJ各120任务，报告期范围为20070101至20071231。
- 正式任务、logical request、canonical API/params请求及pair哈希与先前只读候选完全一致；manifest哈希仅因来源固定版从8b3e更新为960e而变化。与batch1的task、logical、canonical request和pair重叠全部为0。
- plan-only验证360项，五个`would_*`字段全部为false。真实精确scope在44.713秒内完成360次HTTP请求，实算483.081rpm：两个API各113 done/67 empty，共7955行；不发布、不切换`CURRENT`。
- 主闭包验证每个任务的唯一attempt、HTTP 200、raw object、observation、Parquet行数与SHA；独立脚本再次验证946个引用/3098139字节，360 object、360 observation、226 parquet，错误0。
- 当前960e固定版负对照缺全部946个新引用，符合本批在发布后产生的顺序；因此状态仍是authority闭包完成、等待正常publisher与Mac镜像。
- 受控窗口只停止Beat和普通Tushare worker，两者均自然退出0并已恢复；QuantDB、主服务和文档worker持续运行。五个目标服务当前running、restart0、OOM false。
- Authority不可覆盖归档：`validation/top10-holders-batch-20260912/batch-2`；机器证据：`docs/tushare-top10-holders-batch2-20260912.evidence.json`。

非阻塞记录：固定版证据提交后的双端handoff首次遇到Syncthing扫描，idle后复跑通过；一个仅涉及`/tmp`文件属主的错误用户假设被拒绝且未影响authority；独立复核脚本两次SCP握手被重置，稍后有界重试通过。完整持股历史、旧报告迟发修订、空响应完整性、known_at/PIT及RRG/策略结论继续开放。
