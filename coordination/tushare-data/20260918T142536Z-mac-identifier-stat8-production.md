# Mac Tushare 标识对象校验并发优化生产结果

- 时间、节点、任务：2026-09-18T14:25:36Z，Mac 全量归档所有者，identifier-stat8。
- 状态：代码、测试、主分支合入、本地运行时部署及真实生产验收完成；全量历史补采继续。
- 代码：候选 `c33e779e`，主分支 `188784fe`。运行副本与仓库的 `tushare_pipeline.py` SHA-256 均为 `6b90cff03d9b57658551573f010f7a9bf34e353e8fcf0026f91ceddcd1928302`。

本次只把 `identifier_discovery_cache` 命中后的历史对象 `stat` 校验分成最多 8 个固定分片并发执行。每个缓存对象仍逐项核对设备号、inode、大小、mtime 和 ctime；缺失对象继续抛错，指纹不符继续拒绝缓存。规划范围、15 分钟周期、任务检查点、Tushare 分层限速、文档处理和云端数据角色均未修改。

相关 93 项测试通过，另通过 ruff、编译及 diff 检查。部署前对生产缓存做只读完整基准：322,158 个对象全部有效，8 路耗时 15.664 秒。部署时先移除 ENABLED 等待在途采集和文档事务自然提交，再重载本地 LaunchAgent；原 ENABLED 内容与权限恢复，未清空任务、重建数据库或手工修改规划检查点。

固定周期的真实在线验收结果：旧版 322,158 个缓存对象、1,730 个新增结果时，`identifiers` 为 93.069 秒、总规划 99.931 秒、tick 143.436 秒；新版 323,888 个缓存对象全部复核、1,411 个新增结果时，对象 stat 为 10.330 秒、`identifiers` 为 18.464 秒、总规划 24.177 秒、tick 43.240 秒，分别下降约 80.2%、75.8% 和 69.9%。同轮处理 2,500 个真实文档阶段，`failed_stage=null`。

规划后的下一轮正常完成 560 次真实 Tushare 请求和 2,500 个文档阶段，`failed_stage=null`。因此保留该优化。本地无凭据回执为 `validation/identifier-stat8-production-v1.93d980668b4f7510d57435a70c6e2ec49fcf36fcc01bf11dee57b1439a2e1007.json`，文件名与内容 SHA-256 一致，权限 0600；不含 Token、订单信息或其他凭据。

这里的 `planning_only` 是生产任务队列的持久化规划周期，不是 dry-run 或演练。正常上游采集一直由相邻轮次继续执行。
