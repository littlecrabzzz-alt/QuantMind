# Saturated parent-observed identifiers ready
- Owner structured_contracts; isolated codex/tushare-structured; candidate `8da8ed1999127a8947e0c5f490b411f35d18184d`. Based on merged e8ee822; cherry-pick candidate only. Only Pipeline.split_request, its run call, and new scripts/test_tushare_observed_fanout.py; AST confirms all other Pipeline methods unchanged, including normalize. No production operations.
- `split_request(row, job, result=None)` unions existing discovery with legal nonempty source strings in saturation_param from parent response. Filters nonstrings/empty/whitespace/control-containing values, keeps non-A-share vendor identifier shapes without guessing a ticker universe. Result omitted uses row.result. Run passes its freshly captured result before old row.result is persisted.
- Existing identifier fanout may append missing children idempotently. Retains old children, original extra evidence, and unchanged unresolved gap/status. New children use same API/epoch/params plus observed identifier, cycle checked. Existing date plans and global pagination behavior untouched. No new jobs/attempts scans; only existing discovery read plus named parent object and indexed parent-child relations.
- Evidence adds parent_observed_code_count, parent_observed_added_count (= unique source codes absent from current discovery, not total requests newly created), parent_observation. Discovery and observed source sets dedupe. coverage_proven=0/universe_complete=false always; even previously mislabeled resolved fanout is not accepted as complete.
- 7 new tests cover missing codes/source/discovery union, empty/nonstring/whitespace/duplicate source values, future-announcement axis preserved, persisted-result reopen repair, repeat job/child idempotence and old evidence, custom saturation_param, invalid/empty/code-scoped no children, date split precedence, unknown-universe no false completion, and actual MockTransport run->split freshly captured source plumbing.
- 42 isolated observed-fanout/partition-closure/pipeline/equity-event/supplement tests passed. Ruff/diff clean. Source API has_more alone currently does not flag capture saturation below configured row_cap (pre-existing behavior, not changed here); real two 6000-row samples hit their cap. Parent owns deployment/targeted re-entry of retained old saturated parents and source-vs-discovery mismatch audit.
- Continues 20260908T202529Z-mac-observed-fanout-start-structured.md. Implementation ownership released.
