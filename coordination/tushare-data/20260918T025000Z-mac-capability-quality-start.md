# Capability-probe quality retirement start

- Owner: Mac archive integration task.
- Scope: extend the existing retained-quality reassessment script and focused tests in branch `codex/tushare-capability-quality`.
- Production acquisition remains active. Candidate tests use temp-only databases and no credentials or network.
- Goal: reclassify only artifact-verified negative-priority `capability-*` probes that are already backed by an available capability record, a current legal pagination/fanout strategy, and attempted non-capability production jobs. Preserve `possibly_truncated`, attempts, objects, and `coverage_proven=false`; do not claim the formal history is complete.
