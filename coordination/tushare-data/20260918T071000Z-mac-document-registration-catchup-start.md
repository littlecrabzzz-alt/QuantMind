# Mac Tushare 文档注册追赶提速开始

- 负责范围：仅文档注册单轮上限、边界测试、Mac 私有运行配置及生产验收；不动 RRG/持续研究未提交文件。候选分支 `codex/tushare-document-registration-catchup`。
- 索引证据：`attempts_discovery_api` 查询计划为表达式索引搜索。生产 `document_cursor=176248`、attempt 尾部 `631038`；游标之后可注册附件行约 `6,739,713`，其中 `anns_d=6,169,505`、`research_report=538,400`。
- 当前每轮稳定命中 5,000 条上限，最近两轮用时 2.46–2.94 秒，而硬时间预算为 5 秒。按 105 秒周期估算需约 39 小时才能追平当前存量注册。
- 方案：不改变 5 秒截止、持久化游标、100 条事务分块和幂等引用，只扩展可配置记录上限，本地先灰度 10,000 条。验收实际处理量、用时、游标续跑、采集/文档并行吞吐及无 `planning_only` 配置副作用。
