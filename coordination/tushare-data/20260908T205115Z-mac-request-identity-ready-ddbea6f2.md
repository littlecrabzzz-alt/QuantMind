# Immutable request identity ready

Commit 26818851046691ca0b8d47ca240551d9ae2aa1e7; text_contracts releases Pipeline.normalize / store._dataset function blocks and new scripts/test_tushare_request_identity.py. No global imports or other functions changed. Isolated codex/tushare-text merged8249f6c; no production access or original Parquet overwrites.

Contracts with request_identity_fields now retain _raw_row_identity (SHA256 raw source payload), _request_identity (json.dumps selected original request values, ensure_ascii=False,sort_keys=True), _request_identity_status and _request_identity_missing. Generated _row_identity=SHA256(raw_row_hash + newline + canonical request JSON). No derived fields enter upstream params. Missing fields remain incomplete; no defaultP or inference from outputbz_code. APIs without request identity retain the original payload-only hash behavior.

Reader verifies every selected observation reference against fixed release files metadata (bytes/SHA/API), reconstructs request identity in a temporary query table, and projects normalized keys without rewriting immutable files. Legacy _row_identity is interpreted as the known raw-payload hash only when modern provenance fields are absent/null. P/D/I same payload remains distinct; same-type identical reobservations deduplicate across new/legacy format. Changed payload revisions remain distinct. Missing identity/reference, wrong API, unsafe path, checksum mismatch and partial/inconsistent modern provenance raise explicit Request identity gap errors. Returned metadata explains immutable request validation and local projection. Request-provenance verification reads each distinct selected observation once, never upstream or complete raw response bodies.

Parent genericfixture minimum: normal natural-key/_fetched_at/_observation columns, legacy _row_identity=64 lowercasehex payloadhash; observation filename32or64 lowercasehex+.json; fixed manifestfiles contains observations/<name> exactbytes/SHA; observedJSON at least request.api_name=fina_mainbz and request.params.type=P/D/I. Do not weaken production validation for tests. Modern rows additionally require complete derived provenance and matching compound hash.

30 tests pass:7 new request-identity tests,17 existing pipeline tests,6 existing store_extended tests. Ruff format/check and git diff --cached --check pass. Tests cover all threebusiness axes, same-type dedup, mixed legacy/new recovery, immutable-byte preservation, sourcecodes, missingtype, legacy missingmanifest refs, same-size tamper, wrongAPI, partialnewprotocol, sourceobservation symlink and differentbody revisions. Parent owns registration/imports/13API live integration. Worktree clean.
