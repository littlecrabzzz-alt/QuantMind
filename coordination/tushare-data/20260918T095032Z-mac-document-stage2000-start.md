# Tushare：文档阶段 2000 灰度开始

- 时间、节点、任务：2026-09-18T09:50:32Z，Mac，document-stage2000
- 状态：进行中
- 代码：`master` / `ccf6b44def55e6b26794f2100deb4beb5563b49e`
- 范围：只把私有 `document_worker_max_documents` 从 1800 调到代码现有硬上限 2000；100 秒截止、6 路下载、4 路解析、独立文档进程、Tushare 分层限频和日配额均不变。

证据：四路解析上线后连续五个真实生产轮次均处理 1800/1800 个文档阶段且无失败，最近四个有结构化采集的轮次同时完成 594–714 次真实 Tushare 请求。解析槽仍接近饱和，`Pages throttled=0`，数据卷约 2.2 TiB 可用，LaunchAgent 持续运行且未退出。

部署只在原子移走 `ENABLED` 后等待 worker 明确进入 `disabled`，再原子更新 `0600` 配置并原样恢复 marker；不重启运行时或移动数据库。至少观察三个真实轮次，比较阶段完成量、请求数、失败、内存与磁盘；若资源或吞吐退化则恢复 1800。
