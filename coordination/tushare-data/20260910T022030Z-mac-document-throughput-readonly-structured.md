# Attachment throughput after bounded registration — read-only review

- Structured, completed read-only; source master `8193d14`, no runtime ownership/edit, production writes/upstream requests/restarts/config changes = 0. Entry: existing `cloud-compose exec -T quantmind python3 -S -` and bounded worker logs; SQLite `mode=ro`, query_only, per-query 8-second deadline, recent attempts LIMIT2000. Main AGENTS/coord reread; unrelated dirty research/progress files untouched.
- 02:16:01Z snapshot: pending **1,055,751**, parsed **14,392**, blocked **2,561**, retry **8**; downloaded parse_failed76, parse_timeout19, no_text643, encrypted18, HTML/not_applicable230. Both dedicated workers healthy; actual config already two download subprocesses/one parser, max100 phases/90seconds. `processed` counts phases, not successful documents.
- Recent complete20-minute attempt window: 376 completed phase records, 172 distinct successful downloads, 166 distinct parsed documents (~498/hour); 14 pdf.dfcfw source_challenge, six cninfo download_timeout, two content_mismatch, one size_limit; seven parse_failed, three parse_timeout, five no_text. Attempt created_at records claim time, not precise completion time. Separate rolling20-minute logs contained six success receipts, 5–100 phases per~91.6–92.3seconds, plus one database lock failure; windows overlap but are not the same instant.
- Registration is making durable progress: 500 rows/1.195seconds, complete attempt cursor123614, partial123615 offset2741, attempts max124478. Remaining864 attempt IDs are not equivalent to864 missing attachments. Source raw remains stored independently of registration.
- Static arithmetic only: if no new documents arrived and the observed450–500 parsed/hour persisted, present pending would take approximately88–97days. These overlapping short-window rates are not a prediction or a confidence interval. Actual pending grew ~1,824 across19minutes, so the full moving queue has no demonstrated finite clearance ETA; blocked/non-text/encrypted cannot be counted as future parsed success. RRG structured readiness must not wait for all attachments.
- Exact failure task `7d0e6cb0-056d-447f-9cd3-29a56b43d596` at02:02:09Z: `_finish_document` line972 `BEGIN IMMEDIATE` database locked. This is result-persistence contention after processing, not the new registration deadline. Later tasks completed. Competing lock holder is not identified from the evidence; no unsupported attribution to registration/publisher.
- Production EXPLAIN confirms download claim uses `(download_status,retry_after)` then temporary ORDER BY sort across due candidates before LIMIT2. One bounded read-only identical SELECT took0.3975seconds; that is a real repeated cost but does not explain every90-second task. An ordered eligible-claim index/query is a candidate for an isolated million-row test, not a verified production speedup. No code candidate was fabricated without phase timing/migration proof. Per-download/parse/claim/finish timings are still missing; one expensive parser can consume the shared remaining task budget. Reducing parser timeout could strand complex PDFs and is not proposed.
- Disk: available158.340GiB,58.340GiB above preserved100GiB reserve; attachments allocated9,906,233,344bytes and extracted382,976,000bytes. `du` only those two directories, no full-project scan. No growth-based disk ETA from one snapshot.
- Minimal next checks: instrument existing phase and DB-wait durations; test order-preserving eligible claim selection with future retries/claims and bounded first migration; determine long writer lock and preserve raw/result evidence across finish retries. Keep global lock, two transfers, one parser,100GiB guard. Extra consumer is not useful while the lock serializes run_documents.
- Report `/tmp/tushare-document-throughput-review-20260910.json`, SHA256 `3e307a59b7abf2b62ddbbabb9414209595ac4d2a0e88942ea6097957a97101e2`. Includes snapshot, sanitized task receipts, query plan/timing, file evidence hashes. Raw bounded trace `/tmp/tushare-document-lock-trace.txt`; replay helper `/tmp/tushare-documents-live-review.py` (read-only stdout; no file writes in cloud).
