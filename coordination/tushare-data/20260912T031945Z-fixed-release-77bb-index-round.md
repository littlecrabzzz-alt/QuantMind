# Tushare fixed release 77bb index round

- Normal publisher task `5ff83922-3b7e-4d18-ba9c-3002ea240e98` completed in 268.975 seconds as `publish_only`, made zero supplier calls, and atomically published `data-77bb1b4c…`. Its 397,554,805-byte manifest has 906,518 files and 126,008 datasets.
- Cloud exact verification passed all 1,975 references / 46,552,767 bytes from index daily batch 19 and index weight descendant batch 1. The immutable inventory and report are under `validation/fixed-release-20260912/77bb-index-round-*`.
- Mac LaunchAgent run 250 retained the old pointer after the growing manifest exceeded the old 180-second absolute transfer timeout. Commit `8fee86a3` changes only that absolute bound to 900 seconds, retains the 45-second rsync inactivity timeout and all atomic staging rules, passes 13 targeted tests, and was installed with an exact source/runtime hash match.
- Real LaunchAgent run 251 crossed the old timeout, downloaded 4,127 incremental files, verified all 906,518 files and switched CURRENT at 11:16:46 CST with exit code zero and empty stderr. Targeted Mac verification passed all 1,975 references and offline guarded reads returned index daily 3×17 and index weight 3×11 with empty token variables and zero upstream calls.
- Worker and Beat remain healthy with restart count zero and no OOM; the normal queue was empty at acceptance. Cloud has 337,605,636,096 bytes available, 230,231,453,696 above the 100 GiB reserve. The independent full recovery snapshot continues in the background.
- This closes only fixed local availability for these two batches. The 360 new grandchildren, empty results, complete history, revisions, `known_at`, PIT semantics and RRG classification remain open. The roughly 557-second descendant preflight and growing full-manifest scan remain performance follow-ups.
