# Publication checkpoint candidate: ready, default OFF

Owner text_contracts; isolated quantmind-tushare-text / codex/tushare-publication-checkpoint. Baseline0c18fb2; candidate bbc818a pushed. Cherry-pick only bbc818a, not branch merge. Six files: Pipeline.publish + narrow tick publication boundary; new tushare_publish_checkpoint helper; dedicated13tests; bounded metadata benchmark; doc + evidenceJSON. AST outside publish/tick unchanged. No register_documents/identifiers/stock_context changes; no production/source/credentials/config/queue/DB/deployment operations.

Three opt-in recoverable phases: freeze complete candidate, seal standard final manifest, validate lineage/hash and switch CURRENT. Durable private file+journal fsync/replace/directory-fsync, explicit contentSHA, no success checkpoint until final switch. Resume skips live jobs/attempts/partition scans; original rows/attempts/cursors unchanged and newly collected rows appear in next publication. Legacy default unchanged and byte-equal three-generation fixtures include unknown states/APIs, all attempts/revisions, legacy probe archives, nested closure. Failures at content/journal/fsync, retain/JSON, ready, CURRENT and cleanup replay without lost history or premature pointer. Corrupt/symlink/missing/drift reject and preserve evidence. Pending cannot be disabled/delayed by cadence change.

Supplied production traceback location0c18fb2:2977 is document_index(self.root), not ordinary metadata loop. Other timeout ends atjson_bytes(content). Without actual stage timings, last frame cannot prove individual function160s or CPU/memory/lock cause. Existing final-only publish_success_at means full retry; middle checkpoint is genuinely absent before this candidate.

Budget limitation: stage_budget_seconds120 only cooperative between expensive phases; a single scan/document_index/encoding/fsync remains indivisible and may exceed160. Freeze also adds fullcandidate serialization. Keep publish_staged=False; NOT recommended for production activation from this evidence. It requires three beat opportunities and can delay publication/acquisition while pending. No timeout/resources raised. Other passed fixes need not wait for this candidate.

Python3.10.19 final66 related tests passed1.360s; Ruff/diffcheck pass. One earlier test command named a nonexistent archive_retention test; corrected actualretain_reuse suite passes. Metadata-only bounded benchmark100000 file references+100000 children: final24304813bytes and SHA619222b975ce39233c5a9b66d5b871809b9f10391fcb0a41aa794a4b74140782 equal. Legacy0.436s vs staged0.182/0.437/0.009s: total slower and longest phase NOT improved. Not cloud/SLA/source completeness evidence. Benchmark /tmp/tushare-publish-staged-benchmark-20260910.json SHA256 73cb76cb8046553565760f7891b49b32804a5cecdfd187aa93ca730b15356645.

Parent integration only if desired: installer scripts/tushare_mirror.py copy list needs backend/shared/tushare_publish_checkpoint.py one-line addition for offline manualpublisher. This task intentionally did not edit mirror. Do not downgrade to a runtime unaware ofpending journal while one exists; no DB rollback or blind journal removal.
