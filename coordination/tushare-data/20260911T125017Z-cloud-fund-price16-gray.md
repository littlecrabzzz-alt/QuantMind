# Tushare fund price exact batch 16 at 300-request gray tier

- After index daily batch 9 was recorded, the standard no-revoke drain let the active ordinary acquisition finish naturally, confirmed two stable idle inspections, and stopped the Tushare Worker and Beat.
- The batch froze 300 pristine history jobs: `fund_daily` and `fund_adj` for the same 150 open dates from 2009-05-04 through 2009-12-09. Release, calendar, task inventory, config, preparer and helper hashes were pinned; production-image plan-only made zero authority, credential, upstream, write or publish access.
- Execute completed all 300 requests in 45.987 seconds. Every request returned HTTP 200 and all 300 jobs were done, retaining 23128 rows with zero uncertain calls. Recomputed SHA256 passed for 300 objects, 300 observations and 300 Parquet files: 900 unique references and 4813161 bytes.
- Host preparation first lacked `pyarrow` and failed during import before authority or credential access. Production-image preparation then succeeded. After service restoration, an obsolete module name affected only a manual inspect client; the actual configured Celery app subsequently returned pong from all three nodes.
- Worker and Beat are healthy. Free disk remains 113327689728 bytes above the 100 GiB reserve, and `CURRENT` remains fixed release 266b.

The 900 references await a normal fixed release and Mac mirror. Complete fund history, revisions, tradability, `known_at` and PIT remain open. Machine evidence: `docs/tushare-fund-price-batch16-20260911.evidence.json`.
