# Tushare：双 HTTP 采集灰度未保留

- 时间、节点、任务：2026-09-18T10:40:12Z，Mac，acquisition-http2
- 结论：真实生产灰度未获得可测的平均吞吐收益，已撤回配置和代码；全量本地同步继续。
- 灰度代码：`d2105feda6faf3c4457c412f504f488a61940331`、`089cb8493475c217faaf2045212872266d752bf8`。
- 撤回提交：`5af211c0`、`baa57f92`。

## 异常、修复与真实探针

首次部署使用深度 2、2 个 HTTP worker、thread 捕获和保守的 `rollout_account_rpm=480`。第一个生产周期在 100.873 秒后以 `ValueError` 收口，没有产生 attempt 或文档结果；已立即排空 worker 并恢复原配置与原运行时。

在持有流水线锁且只取 4 个请求的受控直接探针中，确认根因是快速通道把已审核的 500 rpm 接口速率与保守灰度值 480 rpm 做了相等比较。候选修复改为“接口已审核上限不低于当前灰度值”，受控探针随后真实完成 4 个请求，没有残留 `inflight` 任务。

## 三轮生产对照

| 双 HTTP 周期 | Tushare 请求 | 文档阶段 | 返回状态 | 峰值进程树 RSS | 失败 |
| --- | ---: | ---: | --- | ---: | --- |
| 1 | 592 | 2,000 | `sample_ok=341, empty=249, truncated=2` | 985.5 MiB | 无 |
| 2 | 328 | 1,845 | `sample_ok=195, empty=130, truncated=3` | 1,074.5 MiB | 无 |
| 3 | 336 | 2,000 | `sample_ok=190, empty=140, truncated=6` | 1,171.0 MiB | 无 |

三轮均无非零供应商错误码，`failed_stage` 为空，结构化结果均由主线程按派发顺序提交。最近五个相邻单 HTTP 生产周期分别完成 380、338、377、494、506 个请求，平均 419.0；双 HTTP 三轮平均 418.667。平均捕获时间也只从 85.606 秒变为 84.841 秒，差异不足以支持增加生产并发复杂度。

## 撤回与恢复验收

在通过自然停机标记等待在途周期完整提交后，LaunchAgent 被排空并重新安装灰度前的 `f970d6793d21f7e0a75aa47b7dd0ef9a60117d70` 运行时。

- 运行时 `backend/shared/tushare_pipeline.py` SHA-256：`c8533cbe112a2bacce8bca183f8f485c97c3a79f0e5a6892fcc3af530afbcdea`。
- 私有配置 SHA-256：`38e5f67f7f75b57d4f0cd43977309f46a0072cdbc2f300a023cb96fd60ad72b8`。
- `ENABLED` SHA-256：`6b45163b577df25f4e6842501fc95128649c5b129daddf3958224864008eda40`。
- 第一轮规划周期完成 1,851 条文档且无失败；紧接的真实采集周期完成 600 个 Tushare 请求和 1,499 条文档，文档下载/解析并发为 6/4，无失败。
- 恢复验收时数据盘可用约 2.18 TiB，未触发 300 GiB 停写或 500 GiB NAS 迁移告警。

最终生产保留单 HTTP worker、深度 2、独立捕获进程、500 次/分钟账户门、每轮最多 2,000 条文档和 4 路解析。Mac 仍是唯一全量归档写入者，云端只保留研究子集/缓存。
