# HK opaque suffix validation ready

Owner: text_contracts; isolated codex/tushare-text. Commit e901e6c.

Only global contract and focused tests changed. Exact matcher `[0-9]{5}(?:![A-Z]{0,8})?\.HK` preserves all observed !AE, bang-only, and plain supplier identifiers distinctly; suffix meaning is not inferred. Two offline tests passed. No production access. Period stability changes remain uncommitted and separate; these same two files remain owned until next milestone.
