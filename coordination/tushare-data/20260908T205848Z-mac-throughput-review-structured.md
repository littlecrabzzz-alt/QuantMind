# Acquisition throughput: bounded read-only review
- structured_contracts; source baseline8249f6c. Only existing cloud-compose exec -T quantmind/tushare-worker read queries, current status/allowlisted nonsecret config, latest200 attempt observations, named CURRENT manifest stat, two <=1s read-only SQL probes and one2s cgroup sample. No acquisition/API calls, production/config/code mutation or worker restart.

Verified facts (2026-09-08 UTC):
- Config: global requests_per_minute240 (configured cap, not a fresh provider-entitlement proof), batch_requests360/batch_seconds90, beat120s. Groups rrg3 and text/structured/market/global/other/supplement/equity_event1 each. One acquire worker/pipeline.lock; per-API independent reservations and learned minimum intervals apply in addition to account gate. Learning currently says hk_daily5 requests/86400s =>17280s, stricter than config3605s.
-20:51:26Z batch260/90.082s;20:53:17Z batch255/89.87s. Active throughput170~173/min; nominal120s cadence gives127.5~130/min. Global pending400220 is growing and is not a fixed-work ETA denominator.
- Latest200 stored captures20:52:27.795..20:54:08.013: request-through-raw-observation time mean0.0546s,p50 .0374,p95 .1366,max .4383. Between-capture gaps p50 .2144,p95 .937,max30.428s. Includes network+response assessment/raw writing, not a pure wire-latency measurement; sample is bounded/recent rather than universal endpoint performance.
- CURRENT manifest43,552,127bytes. Archive recovering tasks92829/remaining47909, lasttick processed4 with5s soft budget; nextpending is a manifest task at cursor25422. This is legacy catch-up, not proof every future tick remains equally slow. No whole-history reread was executed by this review.
- Live EXPLAIN: expand uses jobs_group_pending by state and filters expanded; called per API request. group_next requires TEMP B-TREE for right part of priority,rowid ordering. Single read-only probes in quantmind: expand0rows16.2ms, rrg next25.7ms. These are not instrumented worker-stage totals; worker has different CPU quota.
- Tushare worker2s cgroup sample: memory964042752/1073741824 bytes (~919/1024MiB), CPU usage586625us,3/20 periods throttled111403us, quota .75 core. Short sample cannot establish sustained CPU bottleneck/OOM; it does show little memory headroom for extra concurrent responses.
- Attachment worker is already independent:20:50:58Z90.357s/22phases=16download attempts+6parse attempts, two transfers/one serial parser;560parsed,280507pending. Phase count is not successful-document count. The attachment queue remains a separate major completion bottleneck.

Code-based inference / minimal measurable next steps:
1. Do not start multiple independent acquire workers to bypass240/min. pipeline.lock intentionally rejects overlap; next_job leaves a job pending until run finishes, so naive concurrent calls can duplicate it and uncoordinated clients can violate gates. A future two-inflight controller would need shared serial rate reservations plus durable claims/leases/fencing. Recent p95 capture latency < .25s gives weak evidence for API parallelism being the first win.
2. First add stage timings for initialize/identifiers/plan_extended/archive/expand/gate_wait/capture/normalize/register_documents/publish plus peak RSS to two normal cycles.90s is acquisition-loop budget, not whole-task budget; before/after stages and the120s cadence reduce utilization. Ideal90/120 duty alone limits a240/min active cap to180/min average; current~128~130 leaves additional internal gaps. Consider a slightly longer active batch only after measuring whole-task160/180s deadlines and completion before next tick; do not blindly shorten beat or accumulate duplicate queued tasks.
3. Small SQL candidate: versioned index matching expanded=0 readiness, and a pending/group/priority ordering index that lets rowid break ties without retry_after interposed. Validate EXPLAIN/no temp sort and identical fairness/gates on temp fixtures before production. Current per-request repeat scans multiply with job history; measured single-query numbers only justify a focused experiment, not a claimed total gain.
4. initialize replays all discovered ETF-quarter enqueue attempts every tick; identifiers reads retained discovery results again, plan_extended repeats enumeration with islice from offset. Reuse existing planning_state/input-signature checkpoints and per-tick frozen discovery to avoid unchanged work, preserving newly observed codes and unknown-universe gaps. Profile before redesign; no additional queue needed.
5. publish reconstructs all attempts/files and loads/copies previous manifest, archive_inventory and document metadata; retention may reparse the same previous manifest.43.6MB JSON plus old/new Python structures is a real growth path. First avoid duplicate parse/validation of already-held identical bytes; preserve hashes and every retained file. If still dominant, incrementally index immutable file metadata and stream publication. Never reduce mirror contents or skip archive completeness checks for speed. Moving archive/publication to another writer needs snapshot/lock consistency, so not a safe one-line parallelization.
6. Useful work can proceed independently now: fixed-release RRG signal-slice semantic review and API/Agent consumption acceptance on Mac/isolated readers use zero Tushare quota. Keep broad history running. For attachments, compare success/timeout/unexpected-content per host from retained evidence before raising two-download concurrency; preserve single PDF parser/resource caps.

Conclusion: current verified gap is not simply insufficient network concurrency. First measure/reduce per-tick and per-request local overhead and idle cadence while maintaining existing quota; separately address attachment success throughput. No estimated full-history finish date from growing pending.
