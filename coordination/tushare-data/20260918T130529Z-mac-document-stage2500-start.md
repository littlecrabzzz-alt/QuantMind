# Tushare：文档阶段 2500 灰度开始

- 时间、节点、任务：2026-09-18T13:05:29Z，Mac，document-stage2500
- 状态：开始有界代码调整，生产配置尚未改变。
- 范围：只将 `document_worker_max_documents` 的运行时硬上限从 2000 提高到 2500；不改 100 秒总截止、8 路下载、4 路解析、单文件上限、Tushare 分层限频或 Mac 全量/云端研究缓存边界。

最近六个真实生产周期均命中现有 2000 阶段上限，文档阶段耗时为 79.663–91.445 秒，同时完成 508–616 次真实 Tushare 请求，`failed_stage` 均为空。这说明在不延长现有 100 秒文档预算的前提下，仍有约 8–20 秒的可用时间；现有上限已成为可观测的吞吐限制。

候选必须先通过边界回归、并发完整性测试、Ruff、编译和 diff 检查；合并 `master` 后通过自然周期边界部署，至少观察三个真实周期。如果不能增加完成数、出现 claim/事务不一致或资源异常，则回退生产配置到 2000。
