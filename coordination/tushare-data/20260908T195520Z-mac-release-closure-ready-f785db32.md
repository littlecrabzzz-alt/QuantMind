# Historical release closure helpers ready

Commit 5d216dae19fd66d3eec57207dffb7c3a5656e351; text_contracts releases backend/shared/tushare_archive.py and scripts/test_tushare_archive.py. Isolated codex/tushare-text merged b199eab, clean. Parent owns publisher/manifest_at/mirror integration. No production access.

New retain_release(root, release_id) returns files (predecessor expectations plus all known archived manifest files), archived_releases (ID/path/SHA only) and verification=manifest_verified_references_expected. Exact manifest bytes are copied once to archives/<sha>.json; data IDs are checksum-bound, legacy probe IDs are mapped to observed hash with immutable-ID conflict rejection. Legacy probe results and files-list forms are retained; absent original file metadata produces explicit recovery gap rather than invented original authenticity. Referenced files are not rehashed or inserted as freshly verified by retain_release. Parent must still verify actual bytes/checksums for closure.

recover_archive performs one releases-directory-only catchup for old frozen DBs without release_catchup marker; rescan_releases=True explicitly repeats that discovery without whole-object-tree rescan. Existing bounded task/cursor recovery is reused; no SQLite version change. Interrupted catchup membership rolls back atomically. attachments/<sha>.bin is accepted only as an attachment byte artifact; extracted remains JSON, no parsed-success claim. No old objects/tasks deleted.

13 archive tests +13 existing pipeline tests pass; Ruff and git diff --check pass. Tests exercise seed probe then two later data releases, transfer of only CURRENT.files to a fresh directory without archive.sqlite, historical manifest lookup by hash/mapping, bounded release catchup, interrupted copy/DB registration/catchup, missing referenced bytes, same-size tamper, unsafe paths/symlinks and legacy ID conflict.

Parent integration: before constructing new content, retain CURRENT predecessor, merge returned files with consistency checks, then read archive_inventory so its new release mapping is included. CURRENT manifest mapping should contain all retained IDs. manifest_at can fall back only when direct release path is absent to archives/<data-ID digest>.json with hash/path validation; legacy probe needs saved mapping and cannot infer digest from ID. Mirror verifies files and requires no archive.sqlite. Legacy recovery gaps and reference expected-vs-verified distinction remain explicit. Only one current predecessor is parsed/copied per publish; previous manifests are not recursively embedded/re-copied. Exact raw manifest backups add retained disk bytes as expected; no hardlink assumptions were introduced.
