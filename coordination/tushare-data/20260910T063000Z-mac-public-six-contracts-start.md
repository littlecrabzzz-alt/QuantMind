# Tushare remaining public six contracts start

- Node: Mac isolated worktree; branch `codex/tushare-public-six-contracts`; baseline `957d060b9e19a14125854239a7dc5a90c04980a3`.
- Scope: audit `ggt_monthly`, `rt_min`, `rt_etf_min`, `rt_etf_min_daily`, `tmt_twincome`, and `tmt_twincomedetail` against current official primary pages and retained repository evidence.
- Intended files: extend existing realtime/off-catalog pure contracts, registry/store, coverage ledger/audit tests and focused fixtures; add evidence/ready records. No production config, Token, provider-data request, publication or CURRENT switch.
- Boundary: register only contracts with exact current fields and legal request dimensions; retain independent permission, history, source-time/PIT, universe and saturation gaps. `ggt_monthly` remains blocked unless an authoritative valid endpoint and monthly schema are recovered; `ggt_daily` aggregation is never a substitute.
