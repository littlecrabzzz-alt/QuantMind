# Tushare：文档阶段 1800 生产验收

- 时间、节点、任务：2026-09-18T09:28:30Z，Mac，document-stage1800
- 状态：已验收并保留 `document_worker_max_documents=1800`；全量历史同步仍在进行
- 代码：`master` / `63178bf53d87f4ab42ac15b3c47d5bc55a64f8b6`；本轮只修改 Mac 私有运行配置，未改采集代码或云端存储边界

## 配置与部署

生产连续四轮触及 1600 阶段上限，其中三轮只用 88.536、89.132 和 93.949 秒；四轮均为 `failed_stage=null`。因此只将已有代码硬上限 2000 之内的私有阶段额度从 1600 调到 1800。

- 更新前原子移出 `ENABLED`，等待在途周期自然完成且状态进入 `disabled`；修改配置后原子恢复原 marker，未杀死任务或重启 LaunchAgent。
- 配置 SHA-256：`0b8ea405f35f465dc6a54fc07d4526d7374d7a12365cf82214b1c2e19427236f` → `ba1d4ca5df959f3bfab5f0a345e6477e24aaa48c007324056ffcfee886e67341`，权限仍为 `0600`。
- `ENABLED` SHA-256 保持 `6b45163b577df25f4e6842501fc95128649c5b129daddf3958224864008eda40`。
- 100 秒上限、6 路下载、3 路解析、下载/解析重叠、Tushare 账户/API 分层限频和日配额均未变。

## 真实吞吐

| 周期开始 | 文档阶段 | 下载 / 解析 | 文档耗时 | Tushare 请求 | 失败阶段 |
| --- | ---: | ---: | ---: | ---: | --- |
| 09:09:37Z | 1,369 | 685 / 684 | 100.481s | 308 | 无 |
| 09:11:31Z | 1,800 | 906 / 894 | 100.133s | 524 | 无 |
| 09:13:24Z | 1,800 | 868 / 932 | 100.463s | 556 | 无 |

三轮平均 1,656.3 阶段，比调整前最近连续四轮的 1,600 提高约 3.5%。第一轮因文档难度用尽 100 秒而未触顶，后两轮都用满新额度；这说明新上限只在快轮次增加有效工作，慢轮次仍由 100 秒硬截止限定。

## 固定版发布与恢复

灰度后六小时发布到期，同一 worker 正常生成并原子切换新固定版 `data-8d2a4452cb662844ad72563801f850e287697c0193fd7d6b37d19c81e4b0aee7`。清单为 639,495,684 字节，发布阶段 454.744 秒，全周期 555.448 秒，所有发布子阶段完成且 `failed_stage=null`；同轮还处理 1,523 个文档阶段。`CURRENT` 在清单完成前始终指向旧版，完成后才原子切换。

发布扫描期物理内存峰值约 4.5 GiB，`Pages throttled=0`；发布后主进程 RSS 回落到约 678 MiB。数据卷仍有约 2.2 TiB 可用。随后 `planning_only` 轮处理 1,799 个真实文档阶段；再下一轮已恢复 600 次真实 Tushare 请求和 1,579 个文档阶段，均无失败。

## 范围与队列

当前代码重跑离线范围审计，输出 SHA-256 `c406349c03260af61a5702c5d67061cd6cee0d702d5b8d0b4941ed3d66546812`：250 个命名义务中 247 个为运行时可读接口，没有已注册但 reader 缺失的项目；剩余是已排除的两个账户写操作、SDK 封装和官方页已失效且真实接口拒绝的 `ggt_monthly`。未发现可实施的数据逻辑漏接。

最后一轮结构化队列为 `done=346748`、`pending=2879061`、`blocked=871`、`permission_blocked=4682`；文档为 `pending=2062747`、`parse_pending=97384`、`parsed=74682`。本轮文档登记又识别 8,093 条记录，因此已显式文档待办可以在下载同时继续增长；这是旧 attempt 逐步显式化，不是同步回退。

结论：保留 1800 阶段额度，Mac 继续全量历史同步；云端仍只消费研究子集/缓存。队列仍为百万级，本次验收不代表全量已完成。
