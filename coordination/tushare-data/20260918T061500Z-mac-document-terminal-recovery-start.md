# Mac Tushare 文档终态恢复逻辑开始

- 时间、节点：2026-09-18T06:15:00Z，Mac 完整归档所有者；生产 worker PID 79049 继续运行。
- 当前吞吐：最近完整采集轮 671 请求/100 秒；结构化 pending 2,900,852。文档队列有 1,661,098 pending、84,687 parse_pending、42,994 parsed、7,425 blocked。
- 缺口审计：blocked 中有 5,275 个 `pdf.dfcfw.com` 临时脚本挑战、1,351 个 PDF 内容不匹配、529 个超过 25 MiB、74 个下载超时、37 个 HTTP 错误、28 个尾部空格 URL 和 4 个下载错误。来源挑战的 5,275 份正文归一化后只有一个严格模板；同一旧 URL 当前无 Cookie 真实返回 874,444 字节完整 PDF，证明一次挑战不应永久冻结。
- 实施边界：隔离 worktree 增加低频、有上限的临时失败恢复；保留原结果、tries、attempts 和挑战正文。提高大小上限时只恢复已知可能落入新上限的任务；再次取得的完整 `%PDF-` 正文即使缺少尾标记也保存为 PDF 并进入受限解析，解析失败继续显式保留。不得执行供应商脚本、引入浏览器、读取账号 Cookie 或把异常原文伪装成有效解析。
- 验收：离线测试覆盖调度上限、冷却、证据保留、真实成功、重复挑战、大小上限、尾部空格 URL、本地 malformed PDF 恢复及失败保护；随后在自然周期边界部署，观察 blocked 缩减、真实下载、解析状态和普通吞吐。
