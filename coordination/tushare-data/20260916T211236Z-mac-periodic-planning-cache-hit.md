# 后台周期规划已命中增量缓存

- 时间、节点、任务标识：2026-09-16T21:12:36Z，Mac，Tushare 本地全量归档持续同步。
- 状态：生产验收通过；历史补采继续。
- 提交：接续 master `19aee95f` 及 `20260916T210700Z-mac-planning-cache-publication-live.md`。

launchd 长期进程 PID 37997 在规划到期后自行执行了 `planning_only` 周期，无人工调用和上游请求。标识发现报告 `cache_hit=true`，复用 attempt rowid 301966 之前的 115,882 个对象，只读取缓存后新增的 858 个响应对象；bypassed 184、重复正文 0。标识发现耗时 8.313 秒，24 个规划族总耗时 14.047 秒，完整 tick 15.653 秒，worker 连同并行处理 100 个文档共 20.782 秒；本轮新增 8,980 个任务。

这关闭了“缓存只在手工基准中有效”的风险。待采数量会因周期规划继续发现历史任务而阶段性上升，不能用单轮 pending 是否下降判断停滞或完成。下一步保持唯一 Mac 写入者运行，按固定发布版向云端提供研究子集，并继续消化历史、文档及权限/字段缺口。
