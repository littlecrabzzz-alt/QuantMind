# Tushare：八路文档下载生产验收

- 时间、节点、任务：2026-09-18T10:56:54Z，Mac，document-download8
- 状态：已验收并保留 `document_download_workers=8`；全量历史同步仍在进行。
- 代码：`master` / `a74a1013fea7fadc0699591bf4f01710dc5fe5f5`；本次只更改不进 Git 的私有生产配置。

## 配置与部署

部署前原子移出 `ENABLED`，等待在途周期完整提交且 worker 明确写出 `disabled`；随后原子替换配置并原样恢复 marker。LaunchAgent 没有重启，PID 保持 `71939`、`runs=1`。

- 配置 SHA-256：`38e5f67f7f75b57d4f0cd43977309f46a0072cdbc2f300a023cb96fd60ad72b8` → `5d6cc25972621fec8982b53feedd9d3c2421e057cf6011cef7cf072c5cce6d49`，权限保持 `0600`。
- `ENABLED` SHA-256 保持 `6b45163b577df25f4e6842501fc95128649c5b129daddf3958224864008eda40`。
- 只将下载并发从 6 提高到代码已有硬上限 8；4 路解析、100 秒截止、每轮 2000 条、单文档 256 MiB、Tushare 限速和数据存储边界均未改变。

## 真实生产吞吐

| 周期开始 | 文档阶段 | 下载 / 解析 | 文档耗时 | Tushare 请求 | 失败 |
| --- | ---: | ---: | ---: | ---: | --- |
| 10:51:40Z | 2,000 | 1,014 / 986 | 89.069s | 635 | 无 |
| 10:53:32Z | 2,000 | 959 / 1,041 | 88.823s | 0（`planning_only`） | 无 |
| 10:55:06Z | 1,978 | 1,003 / 975 | 100.149s | 619 | 无 |

三轮平均 1992.7 条，平均文档耗时 92.680 秒，下载/解析任务平均为 992/1000.7。最近四个恢复后 6 路基线周期平均 1472.5 条，因此总阶段吞吐提高约 35.3%。三轮 `processed` 均等于 `finish_db_calls`，没有遗留 claim、失败阶段或非零错误类型。

观察期间进程树峰值 RSS 为 1,308,819,456 bytes（约 1.22 GiB），`Pages throttled=0`，系统容量 128 GiB 且 `memory_pressure -Q` 报告 86% 可用压力余量。数据盘约 2.2 TiB 可用，没有触发 300 GiB 停写或 500 GiB NAS 迁移告警。

## 完整性复核

验收末的结构化队列为 `done=356717`、`pending=2871471`、`empty=305338`、`blocked=871`、`permission_blocked=5847`、`split_pending=9908`；文档为 `parsed=116041`、`parse_pending=97137`、`pending=2231849`。文档待办会随着新的结构化 attempt 继续登记 URL，因此短期总数增长不代表回退。

`permission_blocked` 分布也用只读状态索引复核：5847 条中 5822 条是未购买独立权限的 `cb_price_chg`，`yc_cb` 为 2 条，其余 23 个独立权限探针各1条。没有发现已购新闻、公告、董秘问答、政策法规、券商研报或央行报告被权限状态批量拦截。

Mac 继续作为唯一全量归档写入者，云端仍只拉取研究子集/缓存。队列仍为百万级，本次验收不代表全量同步已完成。
