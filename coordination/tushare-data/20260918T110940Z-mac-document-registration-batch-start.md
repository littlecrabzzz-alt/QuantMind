# Tushare：附件登记批量提速开始

- 时间、节点、任务：2026-09-18T11:09:40Z，Mac，document-registration-batch
- 状态：代码已合入 `master` / `8077eef9`，尚未部署生产运行时。
- 范围：仅将附件登记的 SQLite 事务批次从最多 100 条提高到 1000 条，并将显式 catch-up 的单调用硬上限从 20000 条提高到 100000 条。生产首次部署不修改私有配置，仍为 20000 条/5 秒。

实时索引复核显示，附件登记游标 `177499`，attempt 最大 rowid `685482`，其中真正带附件的待登记 attempt 为 10624 个：`anns_d=6764`、`idx_anns=1362`、`research_report=1185`、`npr=705`、`monetary_policy=608`。最近 10000 个 attempt 中附件结果占 2.86%，原 100 条事务下每轮只能完成约 1–2 个 6000 行公告结果，登记积压会继续增长。

候选没有修改原始响应、行哈希、文档/引用 ID、观测引用、断点顺序或 deadline/SQLite 进度处理。崩溃时可重放的最大已提交块增加到1000条，但引用 ID 确定且提交顺序仍是文档库先于流水线 checkpoint，因此重放不会重复或跳过引用。

离线基准使用一份已保留的 6000 行 `anns_d` 响应和临时 SQLite，五次交错运行的中位时间从 0.479 秒降到 0.186 秒（2.57x）；基线和候选的逻辑文档/引用库存 SHA-256 完全一致。13 项登记专项和 80 项相关测试通过；194 项流水线集合中的两个失败已在未修改基线复现，与候选无关。

生产将等待当前周期自然排空，保持私有配置和 `ENABLED` 不变，重新安装运行时；至少观察三个真实周期的 `processed_records`、完成 observation、checkpoint、失败与结构化/文档吞吐。
