# Independent document consumer ready
- Mac /root/structured_contracts, codex/tushare-structured; commit f28d1a800d5a9824237ff970f4357ff783e06a74. Pick ONLY this implementation commit; 90a711d merely aligns old worktree snapshot baseline to master and must not be picked.
- Changed only tasks/tushare_tasks.py, qlib_app/celery_config.py, compose.cloud.yml, new test_tushare_document_worker.py and snapshot helper two-line writer/active whitelist change. Snapshot ownership release exact source: coordination/dual-node-development/20260908T165909Z-mac-quantdb-scheduled-714717a4.md.
- engine.tasks.tushare_acquire dispatches engine.tasks.tushare_documents to tushare_documents queue (expires110) before tick. Separate consumer concurrency1/prefetch1,1536m/0.5CPU. No new beat. Authority/ENABLED/config and100GiB reserve checks; no token lookup; existing documents.lock/reuse/retries preserved. Atomic document-worker-status.json; broker dispatch failure reported without blocking API; unexpected worker exception records failure then propagates.
- Config: enable_documents=true, document_execution=worker, document_worker_max_documents=100 (1..100), document_worker_max_seconds=90 (0<seconds<=90). Worker soft/hard105/110secs. Parent/global owns disabling inline run_documents while retaining registration; deploy only with that paired change.
- Validation: new8task tests passed (temp dirs, mock Celery/broker, socket/credential access forbidden); existing16document tests passed against parent tree with httpx,pyarrow,pypdf,reportlab; Ruff task/test passed, diff check passed. Snapshot/deploy edits statically checked by the new tests. No production network, credentials or deploy.
- Parent still owns integration, restarting relevant consumers and cloud throughput/resource acceptance. No promise that74kbacklog or20.6day bound is already removed. Existing Celery configuration loading runtime env is unchanged; document task itself does not fetch Tushare token.
