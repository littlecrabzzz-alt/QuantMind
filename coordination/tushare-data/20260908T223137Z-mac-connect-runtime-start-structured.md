# Connect5 runtime 接续
- Mac/codex/tushare-connect-runtime；直接基于fd19af6。
- 归属registry、store合同映射、pipeline.identifiers及connect prerequisites映射/plan_extended开头validation元组；父已确认仅这两个validation小接点，主循环/规划helper/signature不动。mirror仅新增模块名单一行，text已确认本轮不改mirror；父保留c62cb54别名。
- 请求type/market_type/content_type加入runtime identity，并由store复用同一映射；纯合同不改。HK/TI/DC保留原始身份，不强行套股票代码映射。
- 旧4接口证据已发父：当前catalog无旧正文，官方GitHub固定b68d5517...只确认名字/描述，完整旧参数表未找到；当前网页404不能解除历史义务。建议后续最多4个只读schema/权限观察，47旧官方缓存trade_date线索、其余空params不猜字段；本轮不执行真实API。
- 仅隔离Mock测试，无生产操作。
