# Mac Tushare 附件下载持续补位生产验收

- 时间、节点、任务：2026-09-18T04:44:25Z，Mac 全量归档所有者，document-slot-refill。
- 代码：`3d14ea53` 让每个固定下载槽在同一有界批次内完成后立即认领一个替补任务；最多四路、单任务 20 秒、全局 100 秒、持久 claim、失败退避、公开来源保护以及下载批次结束后再解析均保持不变。报告新增 `download_refills`。
- 测试：新增快槽在慢任务结束前补位的确定性测试；文档下载、并行 claim、来源挑战、原生 worker 和归档循环共 65 项通过，Ruff 与 `git diff --check` 通过。

生产部署：

- 旧 PID 33382 在 2026-09-18T04:36:04Z 完整结束 738 次真实 API 请求、204 下载、201 解析、5000 条登记且 `failed_stage=null`。移走 `ENABLED` 后，下一轮于 04:36:09Z 明确写出 `disabled`，文档子进程清空后才 bootout。
- 原 `ENABLED` 原样恢复，SHA-256 仍为 `6b45163b577df25f4e6842501fc95128649c5b129daddf3958224864008eda40`；私有配置未变，SHA-256 为 `87312dec4e74621fa173357702432ef30d72298840a5dfecd30dbd09f2d58c99`，密钥文件权限保持 0600。
- 新 LaunchAgent PID 40497 仅启动一次且未退出；安装副本 `tushare_documents.py` 与 master 文件哈希均为 `fd2c9568737a9d72632be222647f76b8cd8efe23057641d54cf37699efbee6bc`。

真实生产验收：

- 部署后首轮恰逢队列规划到期，结构 API 请求为 0，不计入 API 对比；附件仍真实下载 188、解析 177，发生 140 次补位。此前四路规划周期下载 152，当前规划周期提高约 23.7%。
- 随后三轮真实 acquisition 分别为 744/272/257、757/208/193、765/304/296（API 请求/下载/解析），每轮登记 5000 条且 `failed_stage=null`。API 平均 755.33；下载平均 261.33；解析平均 248.67。
- 四路补位前已验收的三轮真实基线为 API 平均 755.67、下载平均 234.67。补位后 API 基本不变，下载平均提高约 11.4%。下载槽累计空闲从基线三轮平均 208.42 秒降至 153.50 秒，减少约 26.4%；每轮平均发生 196 次补位。
- `source_challenge` 保持 5275；当前 `download_timeout` 为 81，未高于补位前验收的 83；聚合 retry 从部署前完整周期的 66 降为 64。未观察到来源挑战、超时或周期失败恶化。

验收边界结构化 done 303395、empty 263311、pending 2877458；附件 pending 1535542、parse_pending 76226；可用空间 2476399562752 bytes。附件 pending 继续受每轮 5000 条历史登记补入影响，不能把消费提速解释为完整同步已结束。Mac 继续作为唯一全量 writer，云端仍只拉研究子集。
