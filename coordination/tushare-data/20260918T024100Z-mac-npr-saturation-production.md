# NPR capped-history production recovery

- Code: `988543d331c649fcde9cd23779beab0a95e63448` on `master` and `origin/master` adds an explicit `puborg` response-field to `org` request-parameter mapping, legacy date-bisection recovery, and safe skipping of terminal legacy shapes.
- Validation: 91 focused contract/pipeline tests passed in the installed archive Python, including observed fanout, futures, options/other, deferred recovery, market sentiment, bonds, and index saturation. `ruff check` and `git diff --check` passed. A temp replay of the two retained production parents produced `date_bisection` and `observed_value_fanout` with zero upstream calls and an unchanged attempt ledger.
- Deployment: the Mac archive runtime was updated only after a completed-cycle boundary. Installed pipeline/contract hashes match the repository files. `com.quantmind.tushare-archive` returned to `running`; the full archive remained Mac-owned and the cloud full writer stayed retired.
- Production recovery: legacy parent `a3623ea7a14c...` was date-bisected with `upstream_calls=0`; its retained parent remained at one attempt. Its two children were followed through 18 bounded time-split waves and 37 real child requests. The only unique terminal leaf is `org=国务院`, `2008-03-28 08:00:00`, with 500 rows and supplier `has_more=true`.
- Production recovery: legacy parent `5a62ec9faf1a...` was fanned out from four retained `puborg` values with `upstream_calls=0`, `coverage_proven=0`, and `universe_unverified`. Four exact child requests returned: 27 rows for `国务院、中央军委`, 423 for `国务院办公厅`, 5 for `国务院办公厅、中央军委办公厅`, and a terminal 500-row `has_more=true` cap for `国务院` at the same exact second. The parent attempt count stayed one.
- Final evidence: the two epochs each preserve their real terminal exact-second `国务院` cap instead of claiming full coverage. All other new NPR leaves are closed or retained in their date-split ancestry. Production `PRAGMA quick_check` returned `ok`; the global archive worker resumed normal acquisition with two inflight jobs and about 2.884 million pending jobs at the check.
