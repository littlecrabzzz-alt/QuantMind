# Mac Tushare 文档注册追赶提速生产验收

- 代码 `c39c61378af4ff0714b63910fef2ce498a84e583 perf(tushare): raise bounded document registration cap` 已合入并推送 `master`。只将 `document_registration_max_records` 的有界校验上限从 5,000 扩到 20,000；5 秒生产硬截止、100 条事务分块、持久化游标、幂等文档引用和唯一写入者约束不变。
- 开发验证：文档注册预算、tick timing、规划间隔和发布间隔共 44 项测试通过；`ruff`、`py_compile` 和 `git diff --check` 通过。测试证明 20,000 可配置、20,001 及非整数仍被拒绝，已有截止/恢复/重放测试保持通过。ResourceWarning 为已有测试连接清理噪音，未产生失败。

## 生产证据

- 只读索引审计通过 `attempts_discovery_api` 表达式索引查询，没有执行全表扫描。变更前 `document_cursor=176248`、attempt 尾部 `631038`，游标后附件记录约 `6,739,713` 行，其中公告 `6,169,505`、券商研报 `538,400`。5,000 条基线周期用时 2.42 秒，真实请求 610 次，文档下载/解析 120/97，失败阶段为空。
- 在自然周期边界更新 Mac 运行副本。仓库与已安装 `backend/shared/tushare_pipeline.py` SHA-256 均为 `c8533cbe112a2bacce8bca183f8f485c97c3a79f0e5a6892fcc3af530afbcdea`；`ENABLED` 恢复为原 SHA-256 `6b45163b577df25f4e6842501fc95128649c5b129daddf3958224864008eda40`。LaunchAgent 以 PID `5482` 继续运行，未删除或重建生产数据库。
- Mac 私有配置将实际灰度档位设为 10,000 条、5 秒，文档下载 worker 继续为 6。配置从 SHA-256 `03c53cf71ebac913a1ff4f6afaac4ca91a21d0328eb1c72dd6b0c13dd3713f52` 原子替换为 `97745aec837c75918bae536ba1e28f0614d08a2ef7283b482b2db25508095d33`，权限保持 `0600`，不记录 Token。
- 配置切换后恰逢每 900 秒的固定规划到期；该轮原因明确为 `interval_due`，规划指纹保持 `365ac7dea542d0226d352dbff7ce8c11e6a5d20ad84dac2f23a49da31136da4a`，证明文档注册参数没有触发 `configuration_changed`。该轮仍处理 217 个文档阶段，之后 5 秒恢复真实采集。
- 三个连续 10,000 条正式周期的注册用时为 4.38/3.39/4.35 秒，真实 Tushare 请求为 581/627/640，文档阶段为 191/217/287，失败阶段均为空。游标持续推进到 `176257`，当前部分 attempt `176258` 的 offset 为 `2464`，没有回退或重置。

## 继续运行边界

- 验收时游标后仍有约 `6,685,753` 条附件记录；按 10,000 条/105–107 秒的静态存量估算，约 19.5–19.9 小时可追平当前注册积压。此期间结构化采集仍会增加新附件行，所以实际时间可更长，不把静态估算当成完成承诺。
- 文档队列仍在注册追赶期：pending `1,780,219`、parse_pending `87,481`、parsed `47,419`、blocked `6,964`。pending 可随新发现的旧附件上升，不表示已下载文件倒退。当前本地可用空间 `2,454,872,453,120` bytes，低于 NAS 迁移提醒线的条件尚未满足。
- 全量历史同步仍未完成。Mac 继续是完整归档的唯一写入节点；云端只保留研究子集/缓存。
