# Read-only schema3 audit: 9cf3a60 over c62cb54

No runtime changes/production/API access. Candidate exported to isolated directory `/var/folders/6m/svdmp5hd6lj09292v8mt73fw0000gn/T/quantmind-schema3-review-b74unxci`; report `review-report.json`, repeatable `review_schema3.py` and `review_v2_baseline.py`. Run with `uv run --no-project --python 3.10 --with fastapi --with httpx --with pyarrow --with duckdb python <script>` from that directory. Supplier network and DNS denied; mirror transport entirely mocked.

## Confirmed finding

- Cached shard integrity is trusted by publisher. Corrupt cached schema3 state_descriptors bytes in place (same length), then update a document in a different first-prefix: `document_index` reuses damaged descriptor at tushare_documents.py:1377 and Pipeline.publish advances CURRENT; API metadata returns409 checksum mismatch. Existing schema2 leaf cache has the same behavior, confirmed with the c62cb54 writer and identical scenario. No silent API empty-state substitution observed. Mirror checks bytes and rejects before advancing local CURRENT; cloud publisher itself lacks this protection for cached metadata.
- Reported to parent/text; text confirms parent authorized minimal descriptor sha/bytes and leaf-reference/cache consistency checks. Recommend that bounded <=16-descriptor validation, without silently adding per-publication full-state-body rehashes that could undo performance benefits. Existing leaf-byte integrity gap remains explicit independent risk. Re-review text's new delta, not assume this reviewed 9cf3a60 already fixed.

## Compatibility and resource evidence

- 35 selected existing tests pass on actual Python3.10.19: buckets5, index6, evidence5, aliases6, archive13. Custom3 checks + old writer comparative reproduction also pass. Initial broader discovery had unrelated worker import missing optional PyYAML; not reported as full-suite success.
- JSON schema1/v2/v3 API read paths preserved. JSON schema2 production uses SQLite index meta1; text confirms meta2 was unpublished 5c521f1 three-hex flat-root candidate. Correct meta2-three-hex fixture→meta3 needs0 state rewrites and1 descriptor write; normal meta1 migration tested with transactional rollback/reopen. No unsupported Python3.10 syntax/runtime found in changed files.
- Actual Pipeline.publish → archive recovery → mocked fresh Mac mirror → authenticated API: two distinct document IDs share one immutable attachment but retain independent parse statuses and original fetched_at;3refs returned, no duplicate source object or missing descriptors. Archive and mirror consume manifest.files as opaque hashed objects, so no separate index schema interpretation. QuantBot delegates documents reads to the same API; it does not maintain an obsolete independent v2 reader.
- Existing tests verify old fixed release accessible on CURRENT-only fresh mirror through retained archived manifest, missing/cross-bucket/tampered references rejected, migration/descriptor-write failure rollback and retry,20k docs +1k append only expected dirty leaves and single state update only1leaf+1descriptor. No production-scale time/OOM claim.
- Remaining performance limits are existing behavior: state leaves scale with total docs/4096, sufficiently broad write batches dirty most buckets; document_index still aggregates all cached descriptors/file metadata and scans attachment/extracted dirs under BEGIN IMMEDIATE; Pipeline.publish still gathers retained metadata files. New two-level root reduces rewritten descriptor bytes, does not make all publication cost constant. No recommendation to widen task into another index platform.

Not verified: process kill/power-loss fsync durability or real concurrent production latency; transaction failures and reopening are covered, not equivalent to physical disk-failure tests. No new source-code commit from this audit.
