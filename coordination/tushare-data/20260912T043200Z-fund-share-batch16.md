# Tushare fund share batch 16

- Beat stopped first and the active normal acquisition completed naturally before the worker exited. No task was revoked or killed. The exact gate froze 360 pristine `fund_share` requests against fixed release `data-385ae0c5…`, split equally between SH and SZ and spanning 2019-04-06 through 2019-10-03.
- The selection had zero task, logical-key or request-signature overlap with 14 prior manifests containing 4,920 requests. Plan-only accessed no credentials or upstream service and wrote neither authority data nor a release.
- Production completed 360 HTTP-200 calls in 44.282 seconds: 251 done, 109 empty, 59,951 retained rows and zero uncertain calls. Observed throughput was 487.783 rpm. Independent closure verified 971 unique object, observation and Parquet references totaling 6,092,611 bytes by size and SHA256.
- Six artifacts were archived create-only under `validation/fund-share-batch-20260912/batch-16-*`. A queue-length precheck added one leading text line to the operator capture; that line alone was removed before parsing, while the archived execution receipt remained unchanged.
- Worker and Beat were restored from their existing containers and are healthy with restart zero and no OOM. The batch awaits normal fixed publication and Mac mirroring. Empty-result confirmation, complete history, revisions, `known_at` and PIT coverage remain open.
