# realtime8 + replay2 运行候选分工

Mac remaining_markets；base7b8a970，独立worktree quantmind-tushare-realtime-runtime / branch codex/tushare-realtime-runtime。归属registry注册/适配，store日期/可选请求身份最小接点，mirror复制项，新专项test/doc。pipeline仅import、新族配置签名/验证/identifiers与normalize相邻分支；父明确允许run内next_job后capture前新族epoch guard，拒绝旧日/未来/格式错且0HTTP。无next_job/tick/config读取/publish修改（分别保持父/text/structured归属），不动p_list/p_get公共reader。

默认关闭，保留auction历史和其余9快照分流。无生产/探测/启用/重启。候选测试后提交交父统一review。
