# 文本历史可选月窗口优化

- Mac / structured_contracts；分支 codex/tushare-structured；仅修改 backend/shared/tushare_text_contracts.py、scripts/test_tushare_text_contracts.py。接续主目标及文本原契约；代码提交 `073d7411842aba578766b08a93a68ff2c520842c`。
- 本地先 cherry-pick 已在父集成的文本基线为 49e82d5，仅用于建立基线；父不要重复挑此提交，只挑本次月窗口 delta。

调用 config.text_history_window='month'：支持范围的历史按自然月；央行保持年度；CCTV保持逐日。近期仍今天及前6日，来源全集和unknown-prefix保持；问答历史月份附加回复时间轴，与问题时间轴并行。默认/day与旧版完全一致。
验证：7项文本测试及Ruff通过；与旧基线2022-01-25→2024-03-08全部18894任务逐对象一致；新增月模式覆盖全部日期、近期衔接、闰年、每来源、问答双轴、历史前缀与年度央行。不读取凭据/调用API/部署。
静态计划数（1990-01-01→2026-09-09）：day295578→month13608，减少95.4%；这是满额二分前的任务数，不是实际调用耗时承诺，也不缩减目标历史。
父接入必须把text_history_window纳入planning_state签名，避免原day游标直接偏移到短month序列漏任务；保留已存旧jobs/原始版本，重新生成规划游标，重复内容由原存储处理。满额月份继续用父split_request拆分，unknown-prefix无最早界仍独立缺口。
