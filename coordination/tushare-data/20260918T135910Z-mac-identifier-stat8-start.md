# Mac Tushare 标识发现对象校验八路候选开始

- 时间、节点、任务：2026-09-18T13:59:10Z，Mac，identifier-stat8
- 状态：开始有界代码调整，生产运行时尚未替换。
- 范围：只优化规划 cache hit 时对历史内容寻址原始对象的全量属性校验；不降低校验频率、不跳过任何已缓存对象，不改规划、采集、频控、文档或云端角色。

最近一轮 15 分钟规划的 pipeline tick 用时 70.564 秒，其中 `identifiers` 用时 50.636 秒。cache hit 后只需增量处理 1474 个新 attempt，但现有实现仍串行 `stat` 320684 个历史对象；其他单个数据族规划大多低于 0.5 秒。规划实际新增多个数据族任务，因此不通过延长 15 分钟间隔规避该开销。

对当前 cache 中不同 20000 对象的只读样本基准为：串行 2.980 秒，4 线程 0.620 秒，8 线程 0.599 秒，16 线程 0.724 秒。候选使用最多 8 个固定分块，每个对象仍精确比对设备号、inode、大小、mtime 和 ctime；缺失文件必须继续抛错，属性不符必须继续使缓存失效。

候选必须通过并发峰值、精确属性、缺失对象和现有发现/规划回归；生产以下一轮 `planning_only` 的 `identifiers` 实际耗时、总规划耗时、新任务数和失败阶段验收。无收益或资源异常则恢复串行校验。
