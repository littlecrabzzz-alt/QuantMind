# Storage dedup candidates — separate commits, no deployment

Owner text_contracts; isolated codex/tushare-text based on 44d1748. Read-only review predecessor 20260908T220954Z-mac-storage-growth-review-ready-9413e888.md. No production operations, no existing duplicate-file maintenance or history/original/PDF deletion.

Candidate A commit c62cb54: tushare_archive.py, scripts/tushare_mirror.py and new test_tushare_manifest_aliases.py. New retained manifests share the verified immutable release inode via restricted release/archive alias helper. Exact relative-path patterns; pinned directory descriptors and O_NOFOLLOW, regular-file/bytes/SHA and before/after verification; link to temporary name then create-only final name; EXDEV streams a verified copy. Existing mismatching destination never overwritten. Existing absent local archive/release counterparts can be populated without rsync bytes. Mirror still verifies full manifest file inventory before CURRENT, preserves existing valid manifest inode and stores fetched manifest exact bytes (not JSON reserialization). Legacy probe archive mappings remain observed-hash authenticity only. No changes to manifest format, retained paths or publisher self-archive no-op comparison.

Candidate B is the following separate commit on this branch: documents SQLite index metadata1→2 transactionally changes only states triggers/cache/index from two to three hex chars; mappings/attempts/files rowid1000 buckets unchanged. New schema_version2 document index explicitly state_prefix_chars3; historical v2 absent field defaults2. Router _document_page validates2/3 before lookup; current routes have no standalone document-ID state lookup, documents/text remains verified artifact-based. Old shards/indexes stay retained. Initial migration is one-time rebuild; interrupted migration rolls back, interrupted shard save leaves dirty work recoverable. Existing document-index test assertions now read declared prefix.

Validation: manifest aliases6, state buckets3, document index6, pipeline17, archive13, documents16, mirror installer7 =68 tests passed across focused runs; Ruff and diff check passed. All tests isolated temporary directories and mocked/no network. Focused coverage includes actual same inode, synthetic EXDEV, source swap to symlink, corrupt target, interrupted alias publication, raw pretty JSON, mirror alias reuse, legacy fixed index vs new3prefix rows, CURRENT-only fresh client reading old archived index and HTML text, dirty/migration crash recovery, append1000/single-update/noop shard reuse.

IMPORTANT benchmark tradeoff, do not call B an unconditional storage improvement:
- Evidence old: /tmp/quantmind-storage-growth-benchmark-20260909.json. New: /tmp/quantmind-storage-growth-candidate-20260909.json. Both synthetic20k states→21k, same source values; not production throughput estimates.
- Append1000: two-prefix252 changed state shards,6,760,667 new bytes; three-prefix898 smaller shards,2,730,434 bytes (-59.6%).
- Single-state update: two-prefix84,875 new bytes; three-prefix966,799 bytes, because complete top-level state descriptor index is now~965,774 bytes. This is a confirmed tradeoff, not hypothetical. Initial3prefix creates4064 files vs257, more inode/block overhead. Noop new files/bytes0 in both.
- Therefore recommend consider deploying A independently; hold B unless bulk-discovery benefit outweighs per-update index cost, or separately authorize a compact descriptor-index/shard improvement. No unrequested additional index framework was implemented. At current316k production document states, expected dirty fraction for1000 random insertions drops98%→21.7%, but exact production byte savings unmeasured. Huge batches can still touch most buckets; full manifest metadata inventories still grow and all past versions remain required.

All file ownership released after candidate commits; parent chooses later deployment scope.
