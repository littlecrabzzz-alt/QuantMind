# Attachment transfer candidate ready

- Owner: structured_contracts; continues 20260908T191824Z-mac-document-speed-start-9c5b20e9.md. Release implementation ownership after handoff.
- Candidate commit: `9f17a13b8ccdcf7b110c7af0cddbbc984c4b1af3` on codex/tushare-structured. Only backend/shared/tushare_documents.py and scripts/test_tushare_document_parallel.py changed. No production access, deployment, credentials, pipeline/tasks/config edits.
- Invocation: `run_documents(root, max_documents=100, max_seconds=90, download_workers=2)`. Default workers=1 retains sequential path and can parse saved parse_pending from workers=2. Parent controls enabling the new argument.
- At most two download-only subprocesses, 20-second per-transfer total deadline, then one serial PDF parser; parsing never overlaps transfers. Raw files and download result commit before parsing. Completed fast transfer persists while peer waits. Existing URL/DNS/redirect/MIME/25 MiB validation retained.
- Additive document_claim_meta v1 and document_claims durable owner/phase/claimed_at/lease_until; queue PRAGMA remains v2. Global documents.lock retained, expired lease cleanup after deadline plus 30-second grace, stale owner completion fenced. Child CLI enforces own absolute SIGALRM deadline even if parent dies; alarm exit is classified timeout. Original fetched_at/files and phase attempts retained; download-only does not consume PDF parse tries.
- max_documents counts phases in two-worker mode: a PDF download plus parse uses two slots. Five-attempt retry/backoff remains visible. No promise of 100 completed PDFs per tick. 20-second transfer limit may expose slow large downloads as retry/terminal failures; inspect stage counts and failure rates when enabling.
- Validation: all 37 document/parallel/index/worker isolated tests pass; Ruff and git diff --check pass. Includes concurrent overlap, no concurrent parser, raw durability before parse, pending compatibility, claim reopen/expiry/fence, global lock, total budget, independent child alarm, timeout classification, HTTP/DNS mock-only download validation. No network or production fixtures.
- Resource impact: transfer buffering is at most two capped 25 MiB responses, with extra copies/Python/SSL overhead; no exact RSS bound claimed. One parser retains prior 1 GiB address-space and CPU limits. Existing worker container quota unchanged. Synthetic eight PDFs with fixed 40ms download +20ms parse: serial 0.604s, two-download mode 0.410s (~1.47x). This is scheduling evidence, not production throughput forecast; real queue age/download latency/parser CPU remain deployment acceptance work.
