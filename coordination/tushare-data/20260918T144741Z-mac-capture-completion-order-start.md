# Mac Tushare 采集完成顺序调度候选开始

- 时间、节点、任务：2026-09-18T14:47:41Z，Mac 全量归档所有者，capture-completion-order。
- 状态：开始隔离候选；生产继续运行已验收的四路配置。
- 范围：只调整同一有界 capture pool 内已合法预留请求的结果回收顺序；不增加 worker/depth，不改账户/API gate、每日配额、请求范围、重试、原始对象或数据库事务。

当前四路实现按提交顺序 `popleft()` 后立即等待该 future。若队头请求较慢，即使后三个响应已经完成，主进程仍不能提交它们或补入新任务，造成空闲 capture 槽。历史有界样本的单请求网络与原始持久化最大耗时明显高于中位数，当前生产五轮虽稳定但预计请求吞吐仍约 18,229/小时。

候选只在至少一个 future 完成后回收实际已完成的条目；频控槽和 `inflight` 仍在请求提交前由主进程持久化，响应的 attempt/job 事务仍由主进程逐条提交。测试必须证明慢队头未结束时第三个请求可以利用空闲槽、每条任务只提交一次、异常恢复语义和有界并发不变。只有真实生产轮次进一步提高吞吐且无失败、限流、遗留 inflight 或资源异常才保留。
