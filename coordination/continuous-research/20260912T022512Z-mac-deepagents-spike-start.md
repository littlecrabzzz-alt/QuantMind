# Deep Agents 框架短验证
- Mac；用户确认完整方案后，推进第 11.3 节接入验证。
- 范围：独立 worktree `codex/deepagents-validation` 中的验证脚本、独立依赖锁和验收文档；主工作树只写本主题协作记录。相邻平台归档与数据任务文件不修改。
- 真实 GLM API 已获授权；本轮仅短框架/工具验收，无正式研究、模拟、采集或服务发布。
- 输入：临时合成数据与受限测试文件；输出/依赖位于 `/private/tmp/qm-deepagents-spike-20260912`。新增临时 PostgreSQL 和代码容器单独命名/端口，不连接现有业务库、不挂载凭据或共享源码。
- 验证范围：多轮工具、文件/代码、模型/工具中断、消息接续、框架持久恢复；外部作业停止须单独核对。
- 当前主 HEAD `21982240`，已读最新 AGENTS 与方案，其他未提交修改保留。handoff 预检仍只读，不为其结果重启业务服务。
