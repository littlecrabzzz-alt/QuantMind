# 双端开发：已有修复与协作目录起点

- 时间、节点、任务标识：2026-09-08 UTC，Mac，topic-coordination。
- 状态：本轮代码修复与目录约定已完成；正式快照停写耗时待维护窗口验证。
- 已有修复提交：`180e1ac`（主分支；三端 HEAD 与 4,224 个同步文件哈希已在上一轮核对）。
- 本次分工：仅 `AGENTS.md`、`coordination/README.md` 与本条记录；不占用训练、RRG、Tushare 等任务的实现文件。提交后不保留文件归属。
- 方案来源：[开发数据契约](../../docs/development-data-contract.md)、[双端部署记录](../../docs/dual-node-deployment.md)。本目录不复制这些方案。

## 已完成

训练与受管子容器的数据根目录已独立于源码目录；本地启停加入 context、已有卷、重复启动和失败恢复保护；换端 Git 对齐复用 `scripts/dual-node.sh handoff`；快照流程将副本完整校验和冷卷压缩移到恢复写入后。公网配置按用户要求暂不调整。

本轮补充轻量主题记录目录，并把持续更新、文件分工、worktree 接续以及优先复用/避免膨胀写进 AGENTS。没有新增协同脚本、状态系统、锁或自动化。

## 验证与边界

上一轮已通过部署回归、故障注入、临时 Docker 父子挂载验证，以及三端 Git/双端内容核对。原有 7 个未提交研究文件完整保留。未重启正式业务服务，也未运行正式全量快照；不能把代码改进视为停写耗时已经缩短到某个数值。

当前 Syncthing 仅配置主工作树 `quantmind-code`；Mac `.codex/worktrees`、`/private/tmp` 的 worktree 与云端 staging worktree 均在同步范围之外。主工作树中的协作记录可同步，独立 worktree 的未提交代码仍需另行交接。

## 下一步

后续开发者先读已有数据契约和本主题记录。从任意 worktree 通过 `git common dir` 找到共享主工作树，在这里追加自己的独立记录，并声明具体文件分工。

需要验证快照维护窗口时，先确认没有活动研究任务，再从 Mac 主工作树按既有 `snapshot-refresh` 入口创建/拉取；记录输入快照、pause_seconds、服务恢复检查和产物。该运行尚未安排，不自动执行。RRG/Tushare 等独立工作应复用自己的主题和已有方案，先查实际分支/进展，不从本条推断其最新状态。
