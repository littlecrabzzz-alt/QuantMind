# 上游 v2.4.3 集成验收

- 节点：Mac；状态：代码合并完成，服务发布未执行。
- 接续：`20260919T080000Z-mac-upstream-integration.md`；原上游 `qusong0627/QuantMind` 截至 `bf65f66cc944341d1f28e29b20750dbf1c8dc06e`（v2.4.3）。合并提交 `b505cd8c`，纳入同期主分支文档后为 `4a30f938`。
- 分工：本次只做上游集成，不接管 Tushare 采集、研究任务或共享服务发布窗口。RRG 和其他未提交研究修改保持原状。

## 结果

- 纳入上游 352 个提交（相对共同基点 `9ac3a544`），解决 19 个冲突。保留本分支的本地沙盒、自动数据更新、原子行情快照、未复权成交价、独立现金/持仓核算、研究工作台和固定输入保护。
- 训练脚本拆分后，原有标签尾部隔离、独立早停集、OOF 与逐日 IC 指标迁入对应模块，保留本分支筛选默认值；树模型不强制导入 PyTorch。
- 补齐上游取消训练的资源清理及失败重试、精确分区查询的 DuckDB 资源限制、训练镜像缺失模块；保留旧股票池历史表，不执行上游删表语句。
- 本次没有更改公网发布策略。

## 验证

- 隔离容器综合测试：1,027 通过、12 跳过；后续取消失败恢复/查询资源限制定向复测 6 通过。包含小规模真实模型训练、OOF/泄漏检查、交易账本、行情与新股票池测试。跳过项不算通过。
- 前端类型检查、正式构建通过；构建有原有大包体积提醒。双端安全测试 28 项（1 项需显式启用而跳过）、QuantDB 更新测试 10 项通过；两端 Compose 配置校验、Git 差异检查通过。
- 本地业务库只读导出，在断网临时 PostgreSQL 中恢复完整 4.5GB 数据，执行初始化及 v1.0.1–v1.0.8 升级并重复执行，全部成功。原有 121 张表行数未变；用户内容除升级规定的 user_id 规范化外保持一致；无未验证外键。未操作真实数据库。
- 证据仅保存在 Mac `/tmp/quantmind-upstream-*.log`、`/tmp/quantmind-upstream-dbcheck/`；数据库副本含业务数据，禁止提交或同步。

## 后续发布入口与边界

代码合并/同步不代表进程已加载或数据库已升级。要发布时按 AGENTS.md 占用共享服务窗口，确认任务空闲并备份对应节点数据库，再重建受依赖变化影响的镜像、执行升级及相关服务验收；本次未重启本地/云端、未迁移真实库。上游新增 QMT 可选依赖，Windows 执行端、GPU/深度学习和真实交易没有在本次验收，不据此开启交易。
