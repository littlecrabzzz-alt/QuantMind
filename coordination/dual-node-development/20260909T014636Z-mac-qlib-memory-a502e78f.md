# Qlib 缓存内存与完整发布修复
- 状态：进行中；用户授权修复并在云端验收。
- 起点 b23c579；独立 worktree /private/tmp/quantmind-qlib-memory，分支 codex/qlib-memory。
- 分工：backend/services/engine/qlib_data_builder.py 及对应隔离测试，必要的 QuantDB 调度结果/构建入口；复用已有数据规范。暂不占用 Tushare、前端、快照调度文件。
- 已确认：Celery worker 内存限额 1536MiB；旧构建 DuckDB 8GB 加全量 fetchdf，01:51 与 03:07 内核 OOM，日历先写留下半更新。方案是在原构建器内受限流式处理、完整校验后发布，沿用现有路径/作业入口，不增依赖或业务服务。
- 当前 Mac 本地沙盒运行；handoff passed。只在临时目录测试，不改 Mac 固定快照或数据。验证后云端受限真实构建，核对缓存日期/字段与 OOM/内存峰值。
