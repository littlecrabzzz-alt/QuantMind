# 研究数据优先级实施记录（2026-09-25）

## 目标和分层

先交付可复现的 A 股日频量价研究输入，再推进行业强弱与 ETF；总历史、全品种和全文补采继续后台推进。详细边界见 [优先级配置说明](research-data-priority.md)，原始问题分析见 [数据研究链路审计](data-research-audit-20260924.md)。

| 重点 | 当前交付 |
| --- | --- |
| P0：股票行情、复权、估值、日历、基准及交易约束 | 原生调度优先，近七天核心六接口独立入队；云端子集加入涨跌停、停复牌、资金流；15 接口现已独立发布并读通 |
| P1：行业、ETF、课题需要的财务与成分 | 保留调度；行业信号可先验证，历史成分生效时间和财务 PIT 未通过前不视为组合研究完备 |
| P2：全文附件、其他品种、远期全量历史 | 保留原始对象和检查点；正文预算降至每轮 300 个处理阶段/30 秒、4 下载线程/1 解析进程 |

## 第一阶段已修复的实际阻塞

1. 核心行情原先排在数千条股票名称变更任务后面。现在每次规划直接准入所选核心 API 最近七天的请求，上限 42 个，不重置历史游标。实际已完成 9/21–9/24 的六接口采集。
2. 文档失败重试原先对数百万条尝试记录逐文档全表计数。claim schema 7 增加覆盖索引，实库升级耗时 22.5 秒，单文档计数实测 0.214 毫秒。升级没有改写尝试历史。新配置首轮正文约 30 秒，后续一轮处理 300 个阶段，采集轮次实测约 107–123 秒。
3. 云端 QuantDB 原始行情到 9/23，但 PG 和 Qlib 停在 9/18。通过现有带锁入口重建并完整校验 Qlib；PG 按既有幂等 upsert 补入 9/21–9/23 共 16700 行，无失败批次。市场分析也验证为 9/23。
4. CLI/Celery 部分入口原先把派生失败显示为完成。现在复用统一错误判断，将失败阶段记录为 `partial`，CLI 返回非零，Redis 任务标为失败；实际选用的 API 与行情 worker 已重启加载。
5. 固定输入准备新增 `--data-root`，源码与 Mac 沙盒数据可明确分开，避免从旧主目录误取数据。
6. 研究子集从 12 个接口扩展到 15 个时，旧实现会重新校验已有接口的全部文件，首次准备超过云端 30 分钟请求期限。现在可以复用较窄的已验证子集，但要求旧分区全部仍存在、全部文件描述与新发布一致；不满足时回退完整检查。实库预检确认 150245 个旧分区、300491 个旧文件均符合复用条件。新增接口文件仍执行完整校验，损坏的旧清单不能授权复用。修复后源端约 4 分钟内完成 15 接口子集，包含 162623 个分区、325247 个文件，引用本次最新发布版本。

## 已验收的数据

Tushare 9/24 的实际 Parquet 行数：日线 5557、复权因子 5569、估值 5557、涨跌停 5648、停复牌 13、资金流 5557。9/21–9/24 共 24 个分区已逐项核对哈希、行数和日期。各接口口径和证券集合不同，行数无需一致；供应商成功返回不表示全历史完整或 PIT 已验证。详见 [Tushare 分区证据](research-data-priority-20260925-tushare-check.json)。

Mac 全量发布于 01:19（Asia/Shanghai）完成，新版本为 `data-fcbabbb7f133dddab1109d3c130653b46041e2c9f6c9cd53b685d3d28f8ac0ab`，发布过程未调用上游。`ENABLED` 已恢复，原生采集随后持续完成新周期。供数服务的实际启动参数也已从 12 接口扩展到 15 接口。云端于 02:00 完成 24756 个差异文件（约 2.34 GB）的校验与原子切换，子集版本为 `data-0ba57ae05a931a192c45b3615dda9f88ee878743f3c9ede66418eff07d3d9c0d`，`source_lagged=false`，源版本与 Mac 当前发布一致；拉取服务退出码为 0，既有定时器保持运行。缓存总占用约 8.93 GB，低于 50 GiB 预算。

云端随后用应用实际 `read_dataset` 对上述固定子集读取六个核心接口的 9/24 数据：日期与行数全部和 Mac 分区核验一致，单接口耗时约 3.9–20.3 秒，未调用上游。清单发布、传输和应用读取三层分别通过；全历史完整与 PIT 仍保持未验证状态。

云端 PG 实际行数：9/21 为 5565、9/22 为 5567、9/23 为 5568；Qlib 日历和市场分析为 9/23。服务重启后 API 健康检查通过。Mac 本次既有 QuantDB 更新随后自行完成：PG 新增 9/23 的 5568 行，Qlib、市场分析同为 9/23，API 和研究 worker 已恢复。两端源码/Git/健康 handoff 验收通过（`881be655`）。

固定输入 `research-data-priority-20260925` 共 47765 文件、约 4.31 GiB。云端包源码固定在 `e528a6df`，manifest SHA256 为 `2a6a2168d7852f7cbb80e494e91853a3aa6ef6241480fa617a7f2975112d6de7`，位置为 `data/research/inputs/research-data-priority-20260925`。

沿用已有八因子开发比较配置，训练区间为 2023–2024 年，验证为 2025 年一季度，开发比较截止延长至 2026-08-31；行情缓冲文件覆盖至 9/23。用项目实际因子读取器在无网络、只读输入容器中读取 4560471 行，重复键为 0，有成交量但收盘价无效的记录为 0。因子存在少量缺失，数量逐列留档，未作填零或改写。Qlib 两只股票和沪深 300 的 9/18–9/23 共 12 行样本也通过读取核验。

云端和 Mac 本地的新课题默认输入均已切换至该包，两端切换前分别确认活动窗口为 0、研究 Agent 课题为 0；旧输入和旧课题合同保持原样。本地通过新增的 `--data-root .local-dev/project` 准备，47765 个文件的内容哈希与云端逐项完全相同，执行镜像 ID 也一致；各节点保留各自的 manifest 时间与源码提交元数据。两端研究 Agent 均已重新加载并核对实际输入清单。新包仅验证数据交付，本次没有执行训练、策略回测或付费研究调用。详见 [固定输入读取证据](research-data-priority-20260925-read-check.json)。

## 仍然成立的限制

- Qlib 构建报告 131 个标的没有除权事件表，另有少量事件缺行。相关标的的复权因子采用回退口径；需在确定研究股票池时检查，不能把日期追平视为全股票池执行质量通过。
- 历史 ST/涨跌停规则、行业历史成分生效时间、财务公告可得时间仍需按研究方案核验；新增缓存不会自动把这些约束接进既有策略。
- 全量 Tushare 发布仍与正文索引绑定：本次需要重建 4096 个状态桶，历史日志最近五次发布约耗时 19–40 分钟，本次约 34 分钟。第二阶段已分离研究结构化数据的发布节奏，见下文；完整归档继续保留正文一致性校验。云端拉取和实际查询也仍需校验大量历史文件，后续应以固定课题窗口控制输入规模。
- Mac 的 Tushare 休眠可用性、全历史进度，以及本地 QuantDB 应用过程中较长的停机窗口仍是运维限制；不能再让其成为固定输入研究的前置条件。
- 正文 30 秒是阶段间检查的预算，在途阶段不会被强制终止；发布后首个恢复周期正文实测约 40.7 秒，整轮约 103.4 秒，不能把配置值当作严格时限。
- 市场分析后端产物已验收至 9/23；浏览器实际打开停在登录页，尚未完成登录后的页面日期与图表验收。
- 行业与 ETF 的正式组合回测、财务 PIT 因子和分钟/Tick 数据没有在本次宣布就绪。

## 验证与版本

`fb7f77d6`：近期数据调度、正文索引与优先级配置；`e528a6df`：派生状态和固定输入数据根；`881be655`：研究缓存补齐交易约束接口；`90ec0e6b`：扩大接口范围时复用已验证子集。

相关测试：114 项 Tushare 回归，45 项队列/流水线测试，12 项正文 worker 测试，29 项定时同步/派生指标/固定输入测试，9 项研究缓存测试通过。真实验收另包括原生采集、Parquet 校验、云端 PG/Qlib/市场分析和无网络固定输入读取。


## 第二阶段：重点结构化数据独立发布（2026-09-25 上午）

代码 `3aabc7d7` 已合入 master、推送并完成双端源码/Git 核对。原生采集与私有供数进程在自然排空后升级，原 ENABLED 已恢复；云端研究缓存定时器已恢复。未重启研究计算服务、改动既有课题输入或启动策略实验。发布、缓存、质量重评、归一化恢复、归档保留和调度相关 121 项测试通过。

15 个重点接口按成功检查后 900 秒的间隔生成独立 `RESEARCH_CURRENT.json`，使用原有尝试记录、不可变文件、历史分区与内容哈希机制。完整 `CURRENT.json` 及其 6 小时成功检查点分别保留。正文锁繁忙或完整发布失败时，研究模式继续采集，并明确记录延迟/失败；研究发布失败则保留此前指针，报告 partial。

首个实库独立发布于 09:55:59（Asia/Shanghai）完成：研究源版本 `data-4bbdff18a0e466a483ae12efb22c15384024ff1b4c06339622f8fcdb304547dc`，耗时 115.0 秒。其中 274071 次尝试、771815 次文件 stat 检查约 45.4 秒；全库覆盖聚合约 41.4 秒，缺口扫描约 24.5 秒。正文索引没有运行。清单约 201 MB，包含 223715 个结构化分区；上次研究缓存分区全部保留，相对上次缓存新增 7825 个 Parquet 分区版本：index_daily 6069、daily 1699、index_weight 49、fund_basic 8。

与此同时，完整归档指针保持 `data-668ff26b5978ea774d59e4693ad62b71adc17707f44aabd2feae048404f97317` 不变，成功检查点仍为 1790293507。这是结构化数据在完整归档未切换时独立交付的证据。原生采集随后恢复约 106–110 秒的周期，观察到单轮 780 个请求。

云端于 10:06 完成校验并切换至研究子集 `data-076e45d2fad667fdf71e52300260b3dbe557fd3f819147f1e4f8d8daeae34e36`，其 source_release_id 指向上述独立研究版本，source_lagged=false。新增 15650 个文件、275572789 字节，拉取服务退出码 0；缓存总占用约 12.16 GB，低于 50 GiB 上限。随后在云端应用容器中调用实际 read_dataset，对该固定版本读取 9/24 的六个核心接口，行数分别为 5557、5569、5557、5648、13、5557，与源端核验一致，上游请求为 0。六次查询耗时 7.3–33.3 秒，合计约 136 秒。云端文件校验/传输本轮仍耗时约 7 分钟，独立发布并未消除大量历史清单和文件的读取成本。

两条发布路径仍复用唯一采集 writer 和 pipeline.lock。完整归档正在运行时，新的采集与研究发布仍会等待；这次消除的是正文索引对每次研究交付的前置依赖。源端 15 分钟发布周期、云端 15 分钟拉取周期以及校验耗时叠加，不构成 15 分钟端到端延迟保证。缓存仍受 50 GiB 上限约束，不自动删除已有固定研究版本。

项目层面，R01 独立验收另发现 6 项必修：分红权益/到账日、缺行情估值、checkpoint 参数、人工订单重试、缺开盘价回退、独立验收门控。其修复与再验收由原任务负责；数据链路成功不能替代平台/执行验收。本次没有宣布 ETF 回测、PIT、独立策略验证或实盘就绪。

完整检查记录：[独立发布与云端读取证据](research-data-independent-publication-20260925-check.json)。
